{
  "name": "@smart/data",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./state": "./src/state.js",
    "./cache": "./src/cache.js",
    "./pagination": "./src/pagination.js",
    "./repository": "./src/repository.js"
  },
  "scripts": {
    "dev": "echo '@smart/data: No dev server needed'",
    "build": "echo '@smart/data: No build step required — consumers import ESM directly'",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "format": "prettier --write src/"
  },
  "dependencies": {
    "@smart/api": "*"
  },
  "description": "SMART Data — Data access layer with state management, caching, pagination, and repository pattern for SMART Platform",
  "author": "PT SMART VISION INDOTAMA"
}
