# POS.e-Profit — Execution Plan — Log Revisi (Bagian 3)

Bagian 3 dari log revisi `docs/pos/pos_execution.md` (dipisahkan 2026-08-17 agar
dokumen utama tetap ringkas). Berisi entry **v0.46** (2026-08-17):
**F&B Customer Ordering V1** — implementasi penuh M6.3 Table & Order Management,
M6.4 Kitchen Display System (KDS), M6.5 Customer QR Ordering, plus pendukungnya:
Payment Proof, Role-Based Notification Center (bell), Web Push, WhatsApp Notify,
Company Payment Settings (QRIS + Rekening), Company Profile via server POS,
Shift Refund, dan PWA. Entry terbaru (v0.44 – v0.45) ada di `pos_execution.md` §11.

| Versi | Tanggal | Penulis | Deskripsi |
|-------|---------|---------|-----------|
| 0.46 | 2026-08-17 | Freebuff | **F&B Customer Ordering V1 (SP-029 POS V1 — M6.3 Table & Order Management + M6.4 KDS + M6.5 Customer QR Ordering)** — fondasi F&B M6.1/M6.2 kini dipasang jadi alur customer-to-kitchen penuh (sebelumnya backlog butuh approval PO; disetujui & diimplementasikan). Engine-first (R16), seluruh fitur di-gate capability `fnb`. **(1) M6.3 — Table & Order Management.** Model `QrTable` (companyCode, lokasiId/lokasiNama [Warehouse = lokasi operasional], nomorMeja, `qrIdentifier` token internal unik, active; unique (companyCode, lokasiId, nomorMeja)) — QR meja **PERMANEN**: link `https://pos.e-profit.id/m/{qrIdentifier}` dibuat SEKALI (cetak ulang/preview/download tidak membuat link baru). Model `TableOrder` (companyCode, lokasi/meja, `orderNumber` sequential per company + `orderId` "ORDER #000125", orderSource qr_table/pos, items[{productId, kode, nama, skuKode/skuLabel, recipeId/recipeNama, harga, qty, subtotal, catatan, `itemId` stabil per baris, cancelled/cancelledAt/By/Reason}], subtotal/pajak/diskon/total, paymentMethod cash/qris/transfer + paymentStatus pending/paid + confirmedBy/At + confirmationMethod manual, kitchenStatus new/preparing/ready/served/collected/cancelled, cancelledAt/By/Reason + cancelledSubtotal, refundStatus none/pending/refunded + refundAmount/refundedAt/By/Username, `hasKitchenItems` + `kitchenItems` (subset recipe/recipe-fnb utk dapur), readyAt/By, servedAt/By, `orderToken` secret customer, qrIdentifier, catatanOrder, customerWhatsapp, createdBy; unique (companyCode, orderNumber)). Routes `/api/qr-tables` (GET list + `qrLink` dihitung SERVER; POST create; PUT update — identifier TIDAK berubah; DELETE; POST /:id/qr buat link QR; POST /:id/qr/disable) permission `pos.qr.manage`. Routes `/api/table-orders` (GET list filter status pending/paid/ready/completed/all; GET /:id; POST /:id/confirm-pay PENDING→PAID manual idempotent; POST /:id/kitchen ubah status kitchen — status mundur 409, saat READY kirim push+WA customer; POST /:id/refund — order dibatalkan & lunas → refundStatus=refunded + notif customer + audit payment.refunded) permission `pos.order.view`/`pos.order.confirm`. Halaman admin **F&B → QR Menu Meja** (`pages/qr-menu`): CRUD meja + generate/preview/download/cetak QR (individu & SEMUA — template A4 2 kolom) pakai package `qrcode`. Halaman kasir **Order Meja** (`pages/order-meja` + panel EMBEDDED di layar kasir via tombol sidebar 🍽️): filter pending/paid/ready/completed/**batal** (umpan balik user: tab **Batal** baru — SEMUA order dibatalkan [kitchenStatus=cancelled] masuk ke tab ini, dan filter pending/paid kini MENGECUALIKAN order batal `kitchenStatus $ne cancelled` sehingga tab Pending = betul-betul transaksi baru; side effect positif: badge lonceng kasir [jumlah order pending] tidak lagi ter-inflasi order batal), detail, KONFIRMASI BAYAR, Tandai Disajikan, REFUND, **HAPUS order** (icon 🗑️ di kolom Action tab "Semua" — HANYA order belum lunas [paymentStatus≠paid]; `DELETE /api/table-orders/:id` permission `pos.order.confirm`, order lunas/refund = catatan keuangan → 409; bersihkan Notification/PaymentProof/PushSubscription order + audit `order.deleted`). **(2) M6.4 — Kitchen Display System (KDS).** Routes `/api/kitchen` (GET /orders — HANYA order `hasKitchenItems=true` & status new/preparing/ready [filter status opsional]; GET /orders/:id; POST /orders/:id/status [preparing → WA "sedang dikerjakan"; ready → Web Push + WA "sudah siap"]; POST /orders/:id/cancel-items — **pembatalan PER-ITEM** via itemId, refund parsial = subtotal item + pajak proporsional, notif bell kasir + push/WA customer; POST /orders/:id/cancel — batalkan SELURUH order, hanya new/preparing) permission `pos.kitchen.view`/`pos.kitchen.update`. Service pure `order-cancel.js` `cancelOrderItems` (mark item, cancelledSubtotal, refundAmount parsial + pajak proporsional, allCancelled, kitchenRemaining — saat semua item dapur dibatalkan order jadi ready). **Role CHEF baru** (level 25): hanya permission `pos.kitchen.view` + `pos.kitchen.update` — TIDAK bisa settings/payment/void/laporan (RBAC server-side); seed idempotent menambah role bawaan; main.js boot chef langsung ke halaman Kitchen. **M6-FIX: halaman Kitchen STANDALONE utk chef** (umpan balik user) — role `chef` di-boot via `renderKitchenApp()` di main.js: halaman Kitchen dirender TANPA AppShell admin (TANPA sidebar/topbar) di SEMUA jenis device, jadi chef tidak punya menu navigasi & tidak bisa membuka halaman lain (hanya halaman Kitchen + tombol Logout sendiri di header — `kc-logout-btn`, `handleLogout` di-expose `window.__handleLogout`); fallback permission offline chef (`pos.kitchen.view`/`pos.kitchen.update`); admin/owner yang membuka menu Kitchen tetap memakai AppShell normal (deteksi via `isChefStandalone()` = role chef saja). Halaman `pages/kitchen`: grid card order live (auto-refresh 15s), tombol TERIMA / PESANAN SIAP / ✕ BATALKAN SEMUA / ✕ per-item, dark mode, bell kitchen + audio "Ada pesanan baru" (`utils/audio-notify.js` — speechSynthesis suara PEREMPUAN Indonesia + nada Web Audio API). **M6-FIX: board + riwayat + cetak order** (umpan balik user) — (1) **Board hanya order hari ini**: tampilan default = HANYA order HARI INI (semua status) — order dari tanggal sebelumnya (termasuk yang masih aktif/belum selesai) TIDAK tampil di board, hanya lewat tab Riwayat (server `GET /api/kitchen/orders?today=1` → `createdAt ≥ awal hari ini`; umpan balik user: board default harus bersih — riwayat yang tempatnya di tab Riwayat); (2) **Tombol toggle "📅 Riwayat" + filter tanggal**: klik → bar filter `input type=date` (default hari ini) → riwayat order pada tanggal tsb SEMUA status termasuk selesai & batal (`?from=YYYY-MM-DD&to=YYYY-MM-DD`, `parseDate` server, limit 300); tombol berubah jadi "← Order Aktif" utk kembali; (3) **Tombol "🖨️ Cetak" di SETIAP kartu order** — tiket thermal 80mm dengan **kotak checklist ☐ per item** (qty × nama + catatan, meja, order id, status, footer "Centang ☐ pada setiap item saat selesai") agar kitchen bisa mencentang setiap item saat dikerjakan; nama perusahaan di-resolve via `getCompanyByCode` (cache); kartu status selesai (served/collected/cancelled) diredupkan + badge status + pesan selesai/dibatalkan (tanpa tombol aksi). **(3) M6.5 — Customer QR Ordering.** Routes **PUBLIK** `/api/qr` (PUBLIC_RULES method-aware, tanpa login): GET /menu/:identifier — resolve qrIdentifier → company → lokasi → meja (client TIDAK mengirim company/table), gate `fnb` 410, produk trading/recipe/recipe-fnb saja (service TIDAK tampil, dijual≠false, scoped GUDANG MEJA), varian Recipe aktif + SKU + kategori Master SSOT + payment info (QRIS company + rekening aktif) + taxEnabled + whatsapp/waSenderNumber; POST /orders — create order: validasi paymentMethod (qris → QRIS aktif, transfer → ≥1 rekening aktif), **harga item dihitung SERVER dari master Barang/Recipe/SKU** (harga client TIDAK dipercaya — keamanan multi-tenant), itemId per baris, pajak 11% ikut Setting, kitchenItems = produk recipe/recipe-fnb, kitchenStatus new (bila ada item dapur) / ready (murni dagang-jasa), orderToken 18-byte, customerWhatsapp opsional, notif bell KITCHEN "Order baru masuk" + WA customer "Pesanan diterima" (fire and forget); GET /orders/:orderToken — status order + riwayat bukti (sanitize tanpa dataUri) + canUploadProof; POST /orders/:orderToken/proof — **upload bukti pembayaran** (JPG/PNG/WEBP, kompresi client maks 900px q0.7, data URI ≤1.5MB; TIDAK mengubah paymentStatus — PENDING tetap PENDING, 409 bila ada bukti pending). Halaman customer mobile `/m/:identifier` (`pages/customer-menu`, TANPA login — jangan tampilkan login/dashboard): menu + kategori + kartu produk (seluruh kartu bisa diklik, badge Habis, varian/SKU modal pilih), cart, payment (cash/qris/transfer — card TIDAK tampil), create order, status order live (polling 10s + suara/vibrate/notifikasi lokal saat siap/batal/item-dibatalkan), tombol "Beritahu saya saat pesanan siap" (**Web Push tanpa login/HP**), upload bukti + konfirmasi ke kasir, QR MENU GATE (Chrome-first: customer yang akan order wajib izinkan notifikasi dulu — company dengan nomor WA skip gate), **checkout VIA WHATSAPP** (F&B V2 — company punya `whatsapp`/`waSenderNumber`: buka wa.me nomor restoran dgn pesan order otomatis [meja/ref/item/total/nama/No. WA], order DIJADWALKAN server-side: dibuat ±20 detik setelah klik via `POST /api/qr/orders` `waSchedule:true` (fix reliabilitas — sebelumnya dibuat via timer client ±25 detik yang dibekukan OS/browser di sebagian HP [iOS Safari menggantung tab / Android throttling] sehingga order tidak pernah masuk; sekarang pembuatan order di-pegang SERVER — `setTimeout` server + map in-memory `waPendingOrders` — tetap ±20 detik setelah klik [cukup waktu customer menekan Send], dan `DELETE /api/qr/orders/wa/:orderToken` utk batal sementara belum dibuat; client memantau via polling GET /orders/:orderToken dan pindah ke layar status saat order jadi; **notifikasi "pesanan diterima" (WA + Web Push) di-DELAY server-side 30 detik SEJAK order diterima** — `WA_RECEIVED_NOTIFY_DELAY_MS=30000` via `createOrderRecord(payload, { delayReceivedNotifyMs })` — total ±50 detik dari klik, agar balasan "✅ Pesanan Anda telah diterima" TIDAK mendahului kiriman WhatsApp customer [umpan balik user: balasan terlalu cepat saat tanpa delay; 60 detik terlalu lama setelah diuji live — dikembalikan ke 30 detik]; alur WEB (order langsung tanpa waSchedule) TIDAK ter-delay — konfirmasi seketika natural)), mode TAB (`?tab=1` → PWA landscape `manifest-tablet.json` kiosk/self-order). **(4) Payment Proof V1.** Model `PaymentProof` (companyCode, orderId/orderToken/orderNumber/nomorMeja, mimeType, dataUri, status pending/approved/rejected, uploadedBy, verifiedBy/At, verificationMethod manual, rejectionReason; index (companyCode, orderId, status) + (companyCode, status, createdAt)). Routes `/api/payment-proofs` (GET /pending — bukti PENDING utk kasir; GET /order/:orderId — riwayat dgn gambar; POST /:id/approve — PENDING→APPROVED + order PENDING→PAID manual + notif push/WA "Pembayaran dikonfirmasi" + audit payment.proof_approved; POST /:id/reject — PENDING→REJECTED, order TETAP PENDING [safety: upload tidak otomatis lunas], reason opsional + notif + audit payment.proof_rejected) permission `pos.order.confirm`. Service pure `payment-proof.js` (`parseProofDataUri` whitelist JPG/PNG/WEBP + batas 1.5MB, `sanitizeProof` tanpa dataUri utk customer). History bukti DI-PERTAHANKAN (Proof #1 REJECTED → #2 PENDING → #3 APPROVED — audit lengkap). **(5) Role-Based Notification Center (bell).** Model `Notification` (companyCode, locationId, tableId, orderId/orderNumber/nomorMeja, targetRole cashier/kitchen, type order_new/payment_proof/order_cancelled, title/message/payload, read/readAt; index (companyCode, targetRole, read, createdAt)). Routes `/api/notifications` (GET ?role=cashier|kitchen [wajib] & unread=1 — permission ikut role: kitchen → pos.kitchen.view, cashier → pos.order.view; POST /:id/read — user HANYA bisa menandai notifikasi role yang permission-nya dimiliki; POST /read-all?role=). Realtime: polling 15 detik (tanpa WS — ringan). Service pure `notification.js` (`buildKitchenOrderNotification` order_new → kitchen, `buildPaymentProofNotification` payment_proof → cashier [metode+nominal], `buildOrderCancelledNotification` order_cancelled → cashier [+ needsRefund/refundAmount bila lunas], `buildOrderItemsSummary`). Bell KASIR di layar kasir (badge = jumlah notifikasi BELUM DIBACA [order_new + payment_proof + order_cancelled — naik 1, 2, 3... saat order masuk, kembali 0 setelah dibaca/"Tandai semua dibaca"] + suara "Ada order masuk" [order pending naik] + suara "Pembayaran baru menunggu verifikasi" [HANYA saat unread payment_proof naik — tidak dipicu order_new]; LIHAT order_new → buka panel Order Meja, order_cancelled → modal refund, payment_proof → verifikasi bukti) & bell KITCHEN di halaman Kitchen (order baru + [LIHAT ORDER]). **Order baru masuk kini juga membuat notifikasi bell KASIR** (`pushRoleNotification(order, "cashier", "order_new")` di `createOrderRecord` — SEMUA order QR, bukan hanya yang ber-item dapur; `targetRole` diambil dari parameter, builder kitchen tidak menimpa role tujuan). **(6) Web Push (customer, tanpa login).** Model `PushSubscription` (companyCode, orderId, orderToken, endpoint, keys p256dh/auth, userAgent, lastError; unique (companyCode, orderId, endpoint) — **browser yang sama boleh terikat BANYAK order**; `ensurePushIndexes()` drop legacy index {companyCode, endpoint} saat boot — idempotent). Routes `/api/push` (GET /public-key VAPID; POST /subscribe — orderToken wajib valid, upsert per company+order+endpoint). Service `web-push.js`: VAPID keys ENV → `vapid-keys.json` cache → generate (subscription tetap valid antar restart); payload builder `buildReadyPayload`/`buildCancelledPayload`/`buildItemsCancelledPayload`/`buildRefundPayload`/`buildPaymentPayload` (ikon = **PWA statis** `/icons/icon-192.png` — logo data URI membuat payload >4KB → push service tolak 413; `speakText` utk suara di SW); `fitPushPayload` jaring pengaman (strip icon/badge/sound → speakText → body bila >3800 bytes); `sendPushToSubscription` klasifikasi error (400/401/403/404/410 → subscription dihapus; 413 → payload-too-large; lain retryable — kegagalan TIDAK pernah menghentikan alur order). `payment-notify.js` `sendPaymentNotification` (approved/rejected). Service worker `public/sw.js`: push event (notifikasi + suara speechSynthesis id-ID perempuan), notificationclick → deep-link `/m/{identifier}?order={token}`. **(7) WhatsApp Notify (Sidobe gateway).** Service `wa-notify.js`: `DEFAULT_WA_PROVIDER_URL` api.sidobe.com/wa/v1/send-message; `normalizeWaPhone` → E.164 +62; `normalizeMejaLabel`; `statusUrlOf` deep-link; `loadWaConfig` (Company.waProviderUrl/waSecretKey/waSenderNumber → fallback env SIDOBE_*); `loadBankAccounts`; `buildWaOrderMessage` — pesan per event received/preparing/ready/paid/cancelled/items_cancelled/refunded (pesan "diterima" menyertakan instruksi bayar + daftar rekening AKTIF; **notif customer saat order dibuat**: (1) WA "Pesanan Anda telah diterima" + instruksi pembayaran [kasir/QRIS/transfer + rekening aktif] ke nomor WA customer bila tersimpan, (2) **Web Push baru `buildReceivedPayload` "Pesanan Anda telah diterima ✅ — silakan lakukan pembayaran"** ke subscription order (web flow — customer yang sudah izinkan notifikasi), dikirim di `createOrderRecord` (pushOrderReceived) — pada alur WA dikirim TER-DELAY server-side (30 detik sejak order diterima, `WA_RECEIVED_NOTIFY_DELAY_MS`) agar balasan tidak mendahului kiriman WhatsApp customer); `sendWaText` (header X-Secret-Key, is_async, fire-and-forget, TIDAK pernah throw); `notifyOrderWhatsapp`. Settings → **Konfigurasi WA** (`pages/settings/wa`): GET/PUT `/api/pos/settings/wa` (secret TIDAK dikembalikan — hanya `hasSecretKey`; kosongkan input utk mempertahankan, centang "Hapus Secret Key" utk menghapus) + POST `/api/pos/settings/wa/test` (Test Koneksi pakai nilai form saat ini — tidak menyimpan). Event WA: diterima (order dibuat), sedang dikerjakan (kitchen TERIMA), sudah siap (kitchen READY), pembayaran dikonfirmasi (kasir/bukti), dibatalkan (kitchen), item dibatalkan, refund. **(8) Company Payment Settings (QRIS + Rekening).** Model `CompanyQris` (companyCode unique, qrisImage data URI, active — QRIS milik COMPANY, bukan SMART VISION) & `BankAccount` (bankName/accountNumber/accountName/active; unique (companyCode, bankName, accountNumber); **maks 3 rekening AKTIF** — enforcement server POST & PUT 409). Routes `/api/company-payment` (GET/PUT /qris [data URI ≤5MB] + POST /qris/disable; GET/POST /bank-accounts + PUT/DELETE /:id) permission `settings.company.edit`. Halaman Settings → **Payment Settings** (`pages/settings/payment`): upload QRIS company + CRUD rekening. Dipakai customer QR Menu saat checkout (QRIS → tampil gambar; Transfer → tampil rekening aktif). **(9) Company Profile via server POS (FIX).** Routes `/api/company-profile` (GET profil company aktif [auth + company scope, tanpa permission khusus]; PUT whitelist `PROFILE_UPDATE_FIELDS` [jenis/address/phone/email/taxId/logo/favicon/whatsapp/legalId/legalPerdes*/legalAhu/legalNib/legalNpwp/legalInduk/legalIjin/orgPenasehat/orgPengawas/orgKetua/orgSekretaris/orgBendahara] — code/name TIDAK pernah diubah [diatur Console]) permission `settings.company.edit`. FIX akar: halaman Settings → Company sebelumnya memanggil /api/companies/:id di CONSOLE → token POS ditolak (audience isolation SP-027 M3: console expectedAudience=console, token POS ber-audience inventory) → GET/PUT selalu 401 → form kosong & simpan jatuh ke fallback lokal. Sekarang baca/tulis via server POS (token POS valid, DB sama). **(10) Shift Refund (integrasi).** Shift + `totalRefund`/`refundTunai`; `computeShiftRefunds` di routes/pos.js (TableOrder refundStatus=refunded dalam rentang shift, scoped per-kasir via refundedBy/refundedUsername; refundTunai = subset metode cash — mengurangi kas fisik). Tutup shift: totalPenjualan = bruto − totalRefund; penjualanTunai = bruto tunai − refundTunai (expectedCash ikut berkurang). **(11) Misc & hardening.** `Barang` check-duplicate kode scoped PER GUDANG (kode sama di gudang beda = legal) + excludeId saat edit; menu baru **F&B** (QR Menu Meja `pos.qr.manage`, Kitchen `pos.kitchen.view` — capability fnb) + Settings (Payment Settings, Konfigurasi WA — capability fnb); role kasir + `pos.order.view`/`pos.order.confirm` (Order Meja & bell kasir); router route `/m/` TANPA login + `/m/` tanpa identifier → prompt scan QR (jangan jatuh ke login POS); PWA (`manifest.json` QR Menu / `manifest-kasir.json` kasir / `manifest-tablet.json` landscape `?tab=1`, icons 192/512 via `scripts/gen-pwa-icons.cjs`, `sw.js`, meta iOS utk Home Screen); logout icon SVG (Feather log-out — Unicode tidak dirender sebagian perangkat); `Company` POS + `whatsapp`/`waProviderUrl`/`waSecretKey`/`waSenderNumber` (additive). **Verifikasi: full suite 1099/1099 (61 files)** — naik dari 966/966 (54 files, +133 test & +7 file: notification 17, web-push 21, wa-notify 39, whatsapp 19, payment-proof 12, qr-menu 18, order-cancel 7 + regresi pos-transaction 25, transaction-capability 24, company-config 14, company-limits 12, kasir-shell 3, kasir-sku 4, barang-stok 5, pos-gudang 11, recipe 32, src/pages/barang 1) — lint 0 error (warning baseline pre-existing), build pos+inventory+console ✓. Semua fitur F&B di-gate capability `fnb` (nonaktif → menu/route 404 frontend + 410/403 backend). Catatan T13: kasir SUDAH memproses retur/refund order F&B dari layar kasir (REFUND di modal detail Order Meja + modal pembatalan bell — `refundTableOrder`, pengurang penjualan shift). Yang TETAP backlog butuh approval PO: **T13 tombol retur untuk transaksi RETAIL reguler langsung dari layar kasir**. |
| 0.47 | 2026-08-18 | Freebuff | **Laporan: cetak dengan subtotal per halaman + sinkronisasi Laporan Kasir ↔ Penjualan ↔ Laba-Rugi** (umpan balik user) — **(1) Cetak laporan dengan subtotal per halaman.** Module laporan bersama (`packages/smart-inventory-ui/src/modules/laporan/index.js`): `buildPrintHTML` di-refactor jadi **chunked print** — tabel dipecah per halaman (~30 baris, header diulang tiap halaman), **setiap halaman diakhiri baris "Subtotal halaman"** (kolom numerik dijumlahkan), dan **halaman terakhir ditutup baris "TOTAL"** (grand total semua halaman); halaman dipisah `page-break-after: always`; dipakai SEMUA fungsi cetak (stock, purchase, sales, inventory-value, mutation, supplier, customer, labarugi, piutang) — print helper `printToWindow` memakai event `afterprint` untuk menutup window (fallback timeout 1s), `NumericCols` per laporan menentukan kolom mana yang di-subtotal. **Laporan Kasir** (`apps/pos/src/pages/report-pos/index.js`): tombol **🖨️ Cetak** baru — struktur tabel/kolom disamakan dengan modul laporan bersama (header + per-kasir/omzet + rincian transaksi) dan dicetak dengan chunked print (subtotal per halaman + total akhir, `kcStoreRpt`/`storeLaporanKasir`). **(2) Sinkronisasi Laporan Kasir ↔ Laporan Penjualan ↔ Laba-Rugi.** Definisi "Total Penjualan" disamakan: **BRUTO = Σ grandTotal** (tanpa potongan retur/pajak) — angka yang sama di Laporan Kasir (omzet), Laporan → Penjualan, dan Laba-Rugi; kartu summary Penjualan di modul bersama + label Laporan Kasir diganti "Total Penjualan (Bruto)" (`sum.totalBruto`); **transaksi VOID dikeluarkan** dari Laporan Penjualan (`status $ne void` — kasir hanya menghitung status paid; laba-rugi delivered/invoiced/paid), sehingga ketiga laporan tidak lagi selisih untuk periode yang sama; Net Sales tetap dihitung `grandTotal − retur − pajak`. Berlaku di server POS (`apps/pos/server/routes/laporan.js`) DAN inventory (`apps/inventory/server/routes/laporan.js`). **Verifikasi: POS suite 265/265 ✓ · build pos + inventory ✓ · `pm2 restart pos-api` + `inventory-api` (health up) · marker `Subtotal`/`totalBruto` terdeteksi di bundle pos & inventory.** |
