# POS.e-Profit — Execution Plan — Log Revisi (Bagian 1)

Bagian 1 dari log revisi `docs/pos/pos_execution.md` (dipisahkan 2026-08-14 agar
dokumen utama ringkas). Berisi entry **v0.19 – v0.43** (2026-08-10 s.d. 2026-08-13):
M3-FIX v17+ (kuota role-aware, Nota, Member, eye toggle) + M6-FIX v3–v7 (shift wajib,
responsive HP/tablet, kamera barcode, Member NFC, Framework First @smart/core) +
M6.2-FIX (dua model resep, Dijual/Tidak Dijual, action Recipe, Produk VARIAN di Kasir,
VARIAN PRODUK marketplace SKU). Entry terbaru (v0.44+) ada di `pos_execution.md` §11.

| Versi | Tanggal | Penulis | Deskripsi |
|-------|---------|---------|-----------|
| 0.43 | 2026-08-13 | Freebuff | **VARIAN PRODUK Marketplace SKU di Barang Dagangan & Resep Simple (M6.2-FIX)** — user: "bagaimana menambahkan varian untuk type barang dagangan dan resep simple? di modal tambah/edit barang tambahkan radio yg digeser kanan = tampilkan pilihan varian, kiri = tanpa varian; input seperti marketplace (Shopee) — ukuran, warna, topping". Keputusan user (ditanya): **harga per kombinasi** + **stok per kombinasi** (model SKU). Implementasi: (1) **Model Barang POS** — field `varianDef` ([{nama, nilai[]}] — dimensi) + `skus` ([{kode, label, harga, stok}] — tiap KOMBINASI = 1 SKU; label = nilai di-join " / "); additive, barang tanpa varian = kosong. (2) **Form barang (shared module `@smart/inventory-ui`)** — radio **Tanpa Varian / Dengan Varian** (hanya utk tipe `trading` & `recipe`; service & recipe-fnb disembunyikan — resep F&B punya varian sendiri via Recipe): radio on → builder dimensi (nama + nilai dipisah koma, tombol ➕ Tambah Dimensi, hapus 🗑) → tabel kombinasi otomatis (produk kartesian dimensi) dengan kolom **Kode SKU / Harga / Stok** per kombinasi; edit mode prefill harga/stok/kode dari SKU lama (match by label); submit kirim `varianDef` + `skus` (mode off → array kosong = hapus varian lama); badge `N Varian` di tabel/kartu/detail modal; CSS `varian-toggle/dim-row/sku-row`. (3) **Server route barang** — `normalizeVarian`: varianDef dinormalisasi (nama+nilai non-empty), skus (kode wajib, harga/stok clamp ≥0), **stok utama = Σ stok seluruh SKU** (agregat utk laporan/kasir); dipanggil di POST & PUT. (4) **kasir-data** — kirim `varianDef` + `skus` per produk ber-SKU; kartu kasir tetap 1 produk + badge `N Varian` + harga "mulai" (SKU termurah); klik/scan → modal **Pilih Varian** (dropdown per dimensi + ringkasan Harga/Stok live + tombol + Tambah dinonaktifkan saat stok 0); keranjang nama "Produk (label SKU)", harga & `skuKode/skuLabel` tersimpan; checkout/hold kirim skuKode/skuLabel; resume pulihkan; reprice member tidak menimpa harga SKU. (5) **Stok per SKU di transaksi** — helper baru `services/barang-stok.js` `adjustBarangStok` (SKU-aware): item ber-skuKode → update stok kombinasi spesifik + sinkron `stok` agregat; tanpa SKU → `$inc` stok utama (behavior lama); dipakai di **penjualan decrement, void reversal, retur-penjualan** (PATCH returned & DELETE reversal). Penjualan item + `skuKode`/`skuLabel`. Verifikasi: **full suite 126/126** (8 files, +5 test barang-stok: SKU decrement + agregat, reversal, fallback $inc, tanpa SKU, qty 0), lint 0 error (7 warning baseline pre-existing), build pos ✓ (`index-CfQqbNI9.js`) + inventory ✓ (`index-erEukFup.js`; shared vendor live `vendor-inv-ui-BLGuYNOV.js` marker `f-varian`), deploy `pm2 restart pos-api` (health 200), bundle live pos/inv sinkron. Tindakan user: Master → Barang → Tambah/Edit → pilih Barang Dagangan/Resep simple → radio Dengan Varian → isi dimensi (mis. Ukuran: S,M,L) → isi harga & stok tiap kombinasi → Simpan → di kasir produk tampil 1 kartu "mulai", klik → pilih kombinasi. **Follow-up (user): tampilan pilih varian SKU di kasir disamakan dengan resep F&B** — modal SKU tidak lagi dropdown per dimensi + ringkasan + tombol Tambah, diganti **daftar tombol per kombinasi** (label + harga, klik langsung tambah, `.pos-varian-row` yang sama; kombinasi stok 0 dinonaktifkan + label "· Habis"; CSS `pos-sku-dim/summary` dihapus). Client-only: build ✓ (`index-DH1p2q9G.js` live sinkron, marker `data-sku-pick`), lint 0 error (1 warning baseline), tanpa restart server. **Follow-up 2 (user): radio diganti TOGGLE SWITCH + label & foto per SKU** — (a) pilihan Tanpa/Dengan Varian memakai komponen **Switch** (`@smart/ui`, tombol geser): kiri = Tanpa Varian, kanan = Dengan Varian; label kiri/hijau saat off & kanan/ungu saat on; submit baca `input.checked` (bukan radio value); (b) tabel kombinasi SKU kini **berlabel**: header baris `Kombinasi | Foto | Kode SKU | Harga (Rp) | Stok` + setiap baris input punya kolom berlabel (bukan hanya placeholder); (c) **field Foto per SKU (opsional)** — tombol 📷 per kombinasi → pilih gambar → kompres client-side (`compressImage`, maks 3MB) → data URI tersimpan per label (`_skuFotos`) & disimpan ke `skus[].foto` (model Barang + `normalizeVarian` + kasir-data); thumbnail kecil (28px) tampil di modal pilih varian kasir; tombol ✕ hapus foto. Verifikasi: test **126/126**, lint 0 error (7 warning baseline), build pos ✓ (`index-Dh5wEN8S.js`) + inventory ✓ (`index-DX6HdfAv.js`, vendor shared `vendor-inv-ui-CJZ0HlDU.js` marker `varian-sku-foto-btn`), deploy `pm2 restart pos-api` (health 200), bundle live pos/inv sinkron. **Follow-up 3 (user): buang teks toggle, switch hijau+panjang, FIX varian hilang saat edit, label per-field di kiri input** — (a) teks "Tanpa Varian / Dengan Varian" di samping switch **dihapus** (judul "Varian Produk" sudah di atas); (b) switch **hijau #10b981 saat on** (kontras dgn background) + **lebih panjang (96px ≈ lebar judul)**: override `.smart-switch-slider` (width 96px, knob 24px, translateX 66px saat checked); (c) **FIX BUG: data varian hilang saat klik Edit** — akar: `formData` di `openForm` (edit) tidak menyalin `item.varianDef`/`item.skus` → `initVarianSection` melihat "tanpa varian"; fix: formData default & edit kini membawa `varianDef`/`skus`; (d) **label tiap field SKU di KIRI input** (bukan placeholder): tiap baris kombinasi = grid `Kombinasi+Foto | Kode SKU | Harga | Stok` di mana tiap field = `<label>Kode SKU/Harga/Stok</label>` + input (`.varian-field`, label 52px di kiri); (e) **akar label "tidak muncul" = CSS scoped**: modal di-append ke `document.body` (di luar `#barang-page`) sehingga style `.barang-page .varian-*` tidak pernah menjangkau modal — fix: `getVarianStyles()` (selector `#f-varian-field ...`) di-inject `<style>` DI DALAM konten modal (pola `modalStyles` v28f); CSS `.barang-page` lama dihapus. Verifikasi: lint 0 error (1 warning baseline), build pos ✓ (`index-B7mv8JdS.js`) + inventory ✓ (`index-wG7GPSi4.js`, vendor `varian-field` 27×), bundle live pos/inv sinkron (client-only, tanpa restart server). **Follow-up 4 (user): layout SKU 2 baris + kolom isian selebar modal** — tiap kombinasi SKU dipisah jadi **baris 1: nama SKU + foto** (`.varian-sku-top`, label kiri & tombol foto kanan) dan **baris 2: Kode SKU · Harga · Stok** (`.varian-sku-fields`, `grid-template-columns:repeat(3,1fr)` + `width:100%`) sehingga ketiga kolom isian mencapai 100% lebar modal; label tiap field tetap tampil di kiri input. Verifikasi: lint 0 error (1 warning baseline), build pos ✓ (`index-BUghmtaf.js`) + inventory ✓ (`index-B67Rejcg.js`, vendor `varian-sku-fields` 2×), bundle live pos/inv sinkron (client-only). **Follow-up 5 (user): jarak foto-nama & label-input** — `.varian-sku-top` gap 10→16px (foto tidak mepet ke nama SKU) dan `.varian-field` gap 6→12px + label `text-align:right` (label tidak mepet ke kolom isian, jarak konsisten ke input). Verifikasi: lint 0 error (1 warning baseline), build pos ✓ (`index-CZBM6SgS.js`) + inventory ✓ (`index-C32p6iAo.js`), bundle live pos/inv sinkron (client-only). **Follow-up 6 (user): foto ke kiri mepet nama, Harga/Stok format ribuan align kanan, Kode SKU diperlebar** — (a) `.varian-sku-top` `justify-content:flex-start` + gap 8px → foto pindah ke KIRI mepet nama SKU (bukan lagi mepet batas kanan modal); (b) field **Harga & Stok** diganti `type=text inputmode=numeric` + helper `bindThousandInput` (`formatThousand`/`unformatThousand` dari @smart/core): ketik hanya digit, reformat live `xxx.xxx`, kosong bila tanpa angka, `text-align:right`; `readVarianSkus` parse balik via `unformatThousand` (payload tetap angka bulat); (c) **Kode SKU diperlebar** — `.varian-sku-fields` `repeat(3,1fr)` → `1.5fr 1fr 1fr`. Verifikasi: lint 0 error (1 warning baseline), build pos ✓ (`index-DOTeqRld.js`) + inventory ✓ (`index-sRkqXgXp.js`, vendor marker `1.5fr`/`varian-sku-harga` 7×), bundle live pos/inv sinkron (client-only). **Follow-up 7 (user): VARIAN SKU berlaku di INVENTORY utk Barang Dagangan & Jasa** — (1) shared module `BarangModule` + opsi **`varianBehaviorOptions`** (default `["trading","recipe"]` = POS tidak berubah; `varianSupported` membaca state opsi ini); (2) wrapper inventory mengirim `varianBehaviorOptions: ["trading","service"]` → form barang inventory kini menampilkan toggle varian utk Barang Dagangan & **Jasa**; (3) **FIX bug lama**: model Barang inventory TIDAK punya `behavior/dijual/harga_khusus/barcode/varianDef/skus` → mongoose strict membuang field saat simpan (terbukti live: "Ongkir Area 1" buatan inventory tersimpan `behavior: trading` padahal jasa) — model inventory di-mirror persis POS (behavior enum, dijual, harga_khusus, barcode, varianDef, skus+foto); (4) route barang inventory + `normalizeVarian` (POST & PUT, stok agregat = Σ SKU — mirror POS); (5) **backfill data live**: O-001 "Ongkir Area 1" → `behavior: "service"` (diverifikasi modifiedCount 1). Verifikasi: lint 0 error (3 warning baseline), test **126/126**, build pos ✓ (`index-DSoqEsaw.js`) + inventory ✓ (`index-ek4cGKsm.js`, vendor marker `varianBehaviorOptions` 2×), deploy `pm2 restart inventory-api` (health 200, model baru termuat), live pos 200 + inv 200. **Follow-up 8 (user): warna tombol switch varian tidak terlihat (putih)** — track switch saat OFF memakai `#cbd5e1` (abu sangat muda ≈ putih di modal ber-background putih) sehingga tombol tidak terlihat; fix: track OFF jadi **slate-500 `#64748b`** (kontras jelas, knob putih tetap terlihat) + `!important` pada background OFF/ON (`#10b981` hijau) agar menang atas CSS komponen `.smart-switch-slider` (`background: var(--border)`), knob putih + shadow halus. Verifikasi: lint 0 error (1 warning baseline), build pos ✓ (`index-m8cHmOPa.js`) + inventory ✓ (`index-Dnfz8Pkv.js`, vendor marker `#64748b !important` 1×), bundle live pos/inv sinkron (client-only). **Follow-up 9 (user): switch varian HIJAU TUA** — user: "masih putih. coba buat hijau tua. jangan berkreasi sendiri". Dipastikan `<style>` di dalam konten modal (pola `body.innerHTML = content` modal.js) TERBUKTI diterapkan via uji jsdom (computed backgroundColor rgb(21,128,61)); masalah = user melihat bundle lama/cache & warna abu OFF adalah kreasi sendiri. Sesuai instruksi tanpa kreasi: **track switch = hijau tua `#15803d !important` di KEDUA state** (off & checked), knob putih + shadow; warna abu `#64748b` & hijau terang `#10b981` dihapus. Verifikasi: lint 0 error (1 warning baseline), build pos ✓ (`index-CVS2G_q2.js`) + inventory ✓ (`index-CeRYDV6J.js`, vendor marker `#15803d !important` 2×, `#64748b` 0), bundle live pos/inv sinkron (client-only). Tindakan user: **hard refresh paksa** (Ctrl+Shift+R atau buka di tab incognito) — pastikan bundle `index-CVS2G_q2.js`/`index-CeRYDV6J.js` termuat. **Follow-up 10 (user): switch masih putih — TERAPKAN pola biaya-module.js (Bootstrap form-switch), buang komponen @smart/ui Switch** — referensi `shared/uploads/biaya-module.js` modal "Tambah Biaya Baru" (penerima → Manual) memakai **checkbox CSS toggle polos** (`form-check form-switch`), BUKAN komponen framework; Bootstrap ternyata TIDAK ter-load di app pos/inv. Akar: komponen `Switch` @smart/ui memakai CSS custom property `var(--border)`/`var(--primary)` dari tokens — bila tokens tidak ter-resolve di konteks modal, track render putih & override eksternal kalah. Fix: **toggle mandiri 100% inline** — `<label class=varian-switch>` + `<input type=checkbox name=f-varian-mode>` + `<span.varian-switch-track>` (inline `background:#15803d` hijau tua — mustahil putih) + `<span.varian-switch-knob>` (putih, inline); **knob digerakkan via JS** `syncVarianKnob()` (inline `transform:translateX(66px)` saat checked — tanpa CSS `:checked`), dipanggil di init + listener change + syncVarianVisibility; import `Switch` dihapus & CSS `.smart-switch-*` di `getVarianStyles` dibuang (0 sisa di bundle module barang). Verifikasi: lint 0 error (1 warning baseline), build pos ✓ (`index-wTWounLQ.js`) + inventory ✓ (`index-Df01wPgX.js`), marker `varian-switch-track` 1× / `background:#15803d|varian-switch-knob` 3× / `translateX(66px)` 1× di vendor shared, `smart-switch` 0 sisa di module barang, bundle live pos/inv sinkron (client-only). **Follow-up 11 (user): toggle dibuat TIPIS seperti biaya-module.js** — track 96×30 → **96×20** (ramping), radius 10px, knob 24→**16px** (top/left 2px), geseran JS `translateX(66px)` → **`translateX(76px)`** (96 − 2·2 − 16). Verifikasi: lint 0 error (1 warning baseline), build pos ✓ (`index-DdNfP0hF.js`) + inventory ✓ (`index-BDsWLC8T.js`, marker `height:20px|translateX(76px)` 3×), bundle live pos/inv sinkron (client-only). **Follow-up 12 (user): judul "Varian Produk" pindah ke KANAN toggle + toggle diperpendek** — susunan `.varian-toggle` jadi [toggle 44px] + [judul "Varian Produk"] (gap 0.6rem, flex); toggle diperpendek 96px → **44px** (tebal 20px tetap), geseran knob JS `translateX(76px)` → **`translateX(24px)`** (44 − 2·2 − 16). Verifikasi: lint 0 error (1 warning baseline), build pos ✓ (`index-DoggYt3z.js`) + inventory ✓ (`index-CgqA58Wg.js`, marker `width:44px|translateX(24px)` 7×), bundle live pos/inv sinkron (client-only). **Follow-up 13 (user): warna toggle per-app — POS hijau, Inventory UNGU** — opsi baru `varianSwitchColor` di shared module (default `"#15803d"` hijau tua = POS tidak berubah); wrapper inventory mengirim `"#982deb"` (ungu selaras theme inv `linear-gradient(#1e1b4b,#982deb)`); track toggle memakai `state.varianSwitchColor` (inline). Verifikasi: lint 0 error (1 warning baseline), build pos ✓ (`index-vHVxdUqg.js`) + inventory ✓ (`index-CloS2lKO.js`, marker `#982deb` 3× & `varianSwitchColor` di index), POS tetap `#15803d` (default), bundle live pos/inv sinkron (client-only). **Follow-up 14 (user): inventory HANYA menampilkan barang yang DIJUAL** — barang `dijual:false` (bahan resep F&B dari POS) disembunyikan dari seluruh daftar barang inventory: (1) server `apps/inventory/server/routes/barang.js` GET / → `query.dijual = { $ne: false }` (berlaku utk list + search + count; field missing = ikut tampil — backward compat, pola sama dgn filter kasir POS); (2) fallback offline `apps/inventory/src/data/barang-data.js` `listBarangLocal` → `.filter(item => item.dijual !== false)` (konsisten). Data live: PT-001 13 barang, 1 `dijual:false` (kini tersembunyi di inv). Verifikasi: lint 0 error (4 warning baseline), build inventory ✓ (`index-CAnLzzNV.js` + chunk `barang-data-CW6PqSy8.js` live sinkron, marker `dijual`), deploy `pm2 restart inventory-api` (health 200, log bersih). POS Master Barang & kasir TIDAK berubah (tetap menampilkan semua / filter kasir existing). |
| 0.42 | 2026-08-13 | Freebuff | **Batch M6-FIX v7 (2026-08-13): Refactor Framework First — util global & scanner ke smart platform + Deploy 3 Domain** — (1) **`@smart/core` Utils baru** — `packages/smart-core/src/utils/format.js` (`formatRupiah`, `formatNumber`, `formatDecimal`, `parseIdNumber`) & `utils/escape.js` (`esc`/`escHtml`/`escAttr`); diekspor index.js core + facade `SMART.Utils` (Framework First — util lintas aplikasi harus di framework, bukan duplikat per-app). (2) **`formatRupiah` dari app → framework** — definisi lokal di `barang-data.js` (pos & inventory) diganti re-export dari `@smart/core`; 17+ file pemakai via `data/index.js` tak berubah. (3) **`esc`/`escHtml`/`escAttr` disatukan** — ~18 definisi lokal dihapus (halaman app: dashboard/report-pos/shift/pos/rak/inventory pos & inv; framework modules: settings company/role/permission/user, auth login/reset-password, master-crud, smart-inventory-ui dashboard/penjualan/transfer/barang/laporan/pembelian; console `_shared.js` re-export) → import dari `@smart/core`; konsisten `&#039;` untuk quote tunggal. (4) **Anti double-read scan kamera ke `BarcodeScanner` class** (`packages/smart-ui/src/components/scanner/scanner.js`, `_dedupeMs=1500`) — titik terendah yang dilalui SEMUA pemakai (kasir, member, master barang pos/inv, pembelian, penjualan/SO, transfer); `attachScanner.handleScan` kembali polos (dedupe redundant dihapus). (5) **Verifikasi** — test **864/864**, build pos+inventory+console ✓, 0 duplikat `function esc/formatRupiah` tersisa (grep exit 1), tidak ada pemanggilan `esc()` nilai numerik di modul migrasi, lint hanya error baseline pre-existing (`navigator`/`confirm`/`Event` no-undef). Perilaku identik (re-export & satu sumber), refactor murni struktural. **(6) Deploy 3 domain** — build pos+inv+console ✓ → live `index-BrxL5m4i.js` (pos), `index-rOdn8Wrj.js` (inv), `index-BxKTUFvo.js` (master), bundle live byte-identik dengan dist (diff 0), health **200/200/200**. **(7) Audit duplikat lanjutan → framework**: util date/number global baru di `@smart/core` — `formatRupiahID`, `formatThousand`/`unformatThousand` (utils/format.js) + `formatDate`/`formatDateID`/`formatDateTime`/`timeAgo` (utils/date.js), diekspor index.js + facade `SMART.Utils`; ~8 definisi duplikat dihapus (fmt/fmtRupiah/fmtDate/fmtDateTime/timeAgo di inventory page pos & inv, formatDate/formatDateID di penjualan/transfer, fmtNum/fmtRupiah/formatDate di laporan, formatDate/formatThousand/unformatThousand di pembelian, formatDateTime di console `_shared.js` → re-export dari core); verifikasi: test **864/864**, build 3 app ✓, 0 definisi lokal tersisa, lint hanya baseline pre-existing, marker util di bundle live ✓. |
| 0.41 | 2026-08-13 | Freebuff | **Batch M6-FIX v6 (2026-08-13): Member NFC & Datalist, Guard Ganti Member, Scan Pintar, Lebar Bill, Anti Double-Read** — (1) **Field Gudang pindah ke PALING ATAS form Tambah Barang** (module shared `@smart/inventory-ui` — pos.e-profit.id **dan** inv.e-profit.id): urutan Gudang → Kode → Nama → Tipe/Kategori/Satuan/Rak → trading → harga/foto/deskripsi; logika sembunyi saat Tipe=Jasa/Resep tetap terjaga. (2) **Modal Verifikasi Member 4 metode** (scan kamera, tap kartu NFC/Web NFC Chrome Android, dropdown daftar member, input kode manual) — lalu disempurnakan: metode **Tap Kartu pola reader NFC USB keyboard-wedge** (auto-focus, status live "✓ No. Kartu terbaca: … — tekan Enter"), Web NFC tetap fallback (Chrome Android), **input manual dihapus**, metode daftar member jadi **1 field datalist pola "akun biaya"** (`f-member-pick` + `<datalist>`, ketik kode/nama/No. Kartu → saran langsung tampil → pilih/Enter → verifikasi otomatis; guard `_memberVerifying` anti verifikasi ganda). (3) **Field Kode NFC di Master Member** (model Customer pos+inv `kodeNfc`, `check-kode/:kode` & validasi POST penjualan match `$or: kode|kodeNfc`, search list ikut kodeNfc); **kolom tabel "Kode NFC" → "No. Kartu"** (pos & inv); kasir `getMemberByKode` simpan kode asli member. (4) **Fix tombol scan kamera member** — `attachScanner` opsi baru `scanIndexAttr` (default `data-scan-index`; member pakai `data-scan-member`) → tombol scan member bisa diklik (sebelumnya `parseInt(undefined)`=NaN → toggle mati). (5) **Guard ganti member**: member terverifikasi → klik 🎫 Member lagi → alert "Member sudah terverifikasi — batalkan transaksi dulu" (modal tidak terbuka); **Hold/Batal aktif sejak member terverifikasi** (keranjang boleh kosong; `actionsEnabled = cartCount>0 || member`); `cancelCurrentCart` tanpa item → reset member/tipe pelanggan (pesan adaptif). (6) **Kolom pencarian kasir pintar (desktop/tablet)**: ketikan/reader USB yang **cocok persis kode/barcode → langsung masuk bill** (pola sama dengan kamera HP, input dibersihkan), **nama produk → tetap filter grid**; debounce 150ms + Enter langsung proses; kamera 📷 tidak diubah. (7) **Lebar bill**: HP overlay `min(88%,400px)` → **`min(66%,300px)`** (75%), tablet 22% → **25.3%** (115%, produk 78→74.7%), desktop tetap. (8) **Anti double-read scan kamera** — pindah ke `BarcodeScanner` class (`packages/smart-ui/src/components/scanner/scanner.js`): kode sama ter-decode < **1500ms** diabaikan (`_lastCode/_lastCodeAt/_dedupeMs`), kode beda tetap jalan; berlaku **SEMUA pemakai** (kasir, member, Master Barang admin pos+inv, Pembelian, Penjualan/SO, Transfer) karena semua lewat `BarcodeScanner`; dedupe lama di `attachScanner` dihapus (redundan). (9) **Bill header tablet**: judul "Bill" di baris ATAS tombol Umum/Member (`.pos-bill-header` column di 768–1100px) — tidak saling menimpa. Verifikasi: test **864/864**, lint 0 error (1 warning baseline; 2 error `navigator` baseline pre-existing di `_isMobileDevice`), build pos ✓ inv ✓, live bundle pos `index-DGKiZKcf.js` (291.898 B) + inv `index-ChgcoraG.js`, chunk scanner pos `vendor-inv-ui-DukCcw7q.js` (906.603 B) + inv `vendor-inv-ui-v9MbP4EB.js` (917.325 B) identik byte-dengan-dist, marker `_dedupeMs=1500` ×1, health pos+inv **200** (frontend-only, tanpa restart API). |
| 0.40 | 2026-08-12 | Freebuff | **Batch M6-FIX v4–v5 (2026-08-12): Master Barang, Bill & Sidebar Kasir, Responsive HP/Tablet, Kamera Barcode, Fix 401, Badge Kode** — (1) **Master Barang: kolom Barcode dihapus** — kode barang berfungsi sebagai barcode (barcode diisi di kolom Kode); kolom barcode dihapus dari form Master Barang, berlaku di pos.e-profit.id **dan** inv.e-profit.id (deploy kedua domain). (2) **Kasir: 2 titik input barcode** — daftar produk (search + kamera) dan input barcode di kolom bill (ketik/scan langsung masuk keranjang, `addByBarcode` cocokkan kode lalu legacy barcode). (3) **Bill: urutan Total → Bayar** — baris Bayar dipindah ke BAWAH Total (sebelumnya di atas); widget sidebar kiri: tombol **🧾 Tutup** disejajarkan dengan **Kas Awal** (satu baris) + baris **Omset Kasir** (total penjualan kasir sejak buka shift via `GET /pos/shift/summary`, refresh non-blocking & setelah checkout). (4) **Sidebar kategori kasir**: tombol **Sembunyikan/Lebarkan** (pola sidebar admin: class `.collapsed`, persist localStorage, ikon ◀/▶); saat disembunyikan **daftar barang melebar** (bill tetap), kartu produk 5 per baris saat normal & menyesuaikan saat collapsed; jarak antar kategori & spasi bawah Sub Total dikecilkan agar bill menampung lebih banyak item; padding tombol Checkout & Hold dikurangi; bill dipersempit 20% (desktop) dan kolom produk melebar. (5) **Icon kategori**: Master Kategori (admin) + kolom **Icon** (~25 pilihan emoji) setelah kolom Nama Kategori; icon terpilih tampil di sidebar kasir (`kategoriIcons`, fallback 🏷️). (6) **Kategori kasir = Master Kategori (SSOT)** — `GET /api/pos/kasir-data` tidak lagi menurunkan kategori dari `Barang.kategori`; diambil langsung dari koleksi Master Kategori (active, non-archived, urut nama) + icon; fallback offline client juga dari `listKategori`; **4 barang orphan PT-001** (2× Bumbu Instant, Jasa Katering, Ongkos Kirim) di-backup `/srv/backups/kategori-orphan-barangs-2026-08-12.json` lalu dihapus (sisa orphan 0). (7) **Responsive HP & Tablet**: HP (<768px) — bill **disembunyikan** (daftar barang penuh), ikon **🛒 + badge jumlah** di header membuka bill sebagai overlay (slide kanan, 88% lebar / max 400px, shadow, rounded, **#F0FFF0**), tombol ✕ tutup, otomatis tertutup setelah checkout; tablet (768–1100px) — 2 kolom produk 78% + bill 22%; sidebar kategori jadi **strip horizontal di bawah topbar** (kategori digeser 0,5cm dari judul KATEGORI); kartu produk HP **3 per baris**, tablet **5 per baris**; header HP: ikon keranjang & logout **mepet kanan**, hint disembunyikan, nama perusahaan disembunyikan (hanya nama kasir). (8) **Fix error console 401 GET /api/auth/me** — refresh **preventif** (decode klaim `exp` JWT lokal, refresh via httpOnly cookie dulu) sebelum `/me` → `/me` tidak pernah dipanggil dengan token kedaluwarsa (helper `isJwtExpired` + `refreshAccessToken`, `apps/pos/src/main.js`). (9) **Fix 401 saat Tutup Shift — akar masalah di `packages/smart-security/src/middleware.js`**: Express `res.cookie` menginterpretasi `maxAge` sebagai **milidetik** sedangkan kode mengirim **detik** (7 hari = 604.800) → cookie refresh hanya bertahan ±10 menit → kasir jeda >10 menit (buka pagi, tutup siang) → `POST /auth/refresh` 401 → semua request (termasuk `shift/summary`) 401 "token tidak valid". Fix: `maxAge: maxAgeSeconds * 1000` — cookie refresh benar-benar 7 hari (berlaku juga inventory & console, shared package); restart pos-api. (10) **Kamera barcode belakang (HP)** — `BarcodeScanner` (@smart/ui shared, juga Master Barang admin): pilih kamera belakang via `deviceId` (label back/rear/environment/belakang) / `facingMode: environment`; **reset instance `Html5Qrcode` setelah start gagal** (tanpa `clear()`, percobaan berikutnya selalu gagal "already started"); ganti `Html5Qrcode.getCameras()` (membuka+menutup stream kamera → di Android mengunci kamera → `NotReadableError`) dengan **`navigator.mediaDevices.enumerateDevices()`** polos (tanpa sentuh kamera); delay 250ms antar percobaan; log error asli sekali (NotAllowedError/NotFoundError). Tombol kamera juga ditambahkan di kolom Bill (selektor `[data-scan-bill]` khusus agar tak bentrok). (11) **Hapus kolom scan barcode di Bill** — karena scan di daftar produk sudah langsung masuk bill, input + tombol kamera + scanner bill dihapus total (HTML, JS, CSS); kartu produk: badge kategori diganti **kode produk** (`pos-badge-kat` menampilkan `p.kode`). (12) **Follow-up badge & tombol tutup bill**: radius badge `999px → 6px` (huruf awal/akhir kode terpotong), font badge **align center vertikal** (`display:inline-flex; align-items:center; line-height:1.2` + `.pos-prod-meta align-items:center`); tombol **✕** di header bill HP dipindah ke **kiri judul "Bill"** (wrapper `.pos-bill-header-left`), background **merah #dc2626 + X putih**, hover #b91c1c (tetap hidden di desktop/tablet). Verifikasi: lint 0 error (1 warning baseline), **full suite 864/864** (48 files), build pos ✓, deploy live bundle `index-Cdj8eO8w.js` sinkron dist (284.934 B, marker `pos-bill-header-left`, `border-radius:6px`, `background:#dc2626`), chunk scanner `vendor-inv-ui-Dm7G2R2R.js` (906.069 B, marker `enumerateDevices`), restart pos-api (routes/pos.js + smart-security) health 200. |
| 0.39 | 2026-08-12 | Freebuff | **Ringkasan Tutup Shift: Saldo Awal + Penjualan Tunai/Non-Tunai + Selisih (M6-FIX v3)** — modal Tutup Shift kini menampilkan info ringkasan shift sebelum kasir mengisi kas fisik: saldo awal, penjualan tunai (cash), penjualan non-tunai (transfer/qris/card), total penjualan, dan **Kas Diharapkan = saldo awal + penjualan tunai** (pembayaran non-tunai tidak menambah uang fisik di laci). Selisih live = actual − kas diharapkan ditampilkan saat kasir mengetik (✓ pas / ➕ lebih / ➖ kurang). Server: (1) `apps/pos/server/models/Shift.js` + field `penjualanTunai` & `penjualanNonTunai`; (2) `POST /shift/close` agregasi kini split tunai/non-tunai via `$cond` pada `metode_bayar`, `expectedCash = kasAwal + penjualanTunai` (perubahan semantik dari sebelumnya kasAwal+totalPenjualan), breakdown disimpan di shift; (3) route baru `GET /shift/summary` — ringkasan shift open per-kasir (kasir = identitas JWT/header; admin/owner bisa target `?kasir=&kasirUsername=` dari dropdown halaman Shift), 404 bila tak ada shift. Client: `apps/pos/src/pages/pos/index.js` & `apps/pos/src/pages/shift/index.js` — `closeShiftModal` fetch ringkasan saat dibuka (loading → data), dropdown kasir admin memicu refetch summary, selisih live; CSS baru `.shift-summary*`/`.shift-selisih*` di `getStyles()` & `pos-styles.js`. Verifikasi: syntax OK, lint 0 error, build ✓ (bundle `index-CI2sBnpM.js` live, marker `Kas Diharapkan`/`shift-selisih` via curl), restart pos-api health 200, test pos-gudang/kasir-shell/pos-transaction ✓. Hasil review: normalisasi target di `closeShiftModal` shift page (terima `{name,username}` ATAU shift mentah `{kasir,kasirUsername}`) + agregasi `$in ["cash", null, ""]` agar data legacy tanpa `metode_bayar` dihitung tunai (default model cash). **Hotfix (500 di `/shift/summary`)**: `$nin` TIDAK valid di ekspresi agregasi MongoDB (hanya query filter) → `MongoServerError: Unrecognized expression '$nin'` saat modal tutup shift dibuka → diganti `$not: { $in: [...] }` di KEDUA agregasi (`/shift/summary` & `/shift/close`); tambah `console.error` di catch summary agar error terlihat di pm2 log. Terverifikasi: repro agregasi terhadap DB nyata AGG OK (tanpa error), syntax OK, restart pos-api health 200, log bersih, test ✓. **Lanjutan (follow-up user)**: tabel riwayat shift (halaman Shift admin) kini menampilkan kolom **Tunai** & **Non-Tunai** di antara Penjualan dan Expected (`apps/pos/src/pages/shift/index.js` renderTable) — data dari field `penjualanTunai`/`penjualanNonTunai` yang disimpan saat close; shift lama tanpa field tampil "Rp 0" (formatRupiah guard null/undefined). Verifikasi: lint 0 error, build ✓ (bundle `index-DO77JNO4.js` live). **Lanjutan (follow-up user)**: modal "Buka Shift — Wajib" (muncul otomatis setelah kasir login tanpa shift) kini footer berisi **2 tombol**: **Buka Shift 49% + Logout 49%** (spasi 2%, container `.shift-open-actions`) — jalur keluar untuk kasir yang terlanjur login tapi batal buka shift (modal non-dismissable; Logout satu-satunya jalan). Tombol Logout: `overlay.remove()` lalu `requestKasirLogout(() => shellConfig.onLogout())` — reuse guard shift yang sama dengan tombol Logout header (kasir tanpa shift → langsung logout; dengan shift → tutup shift dulu). Modal non-wajib (dari widget, ada tombol Batal) tidak berubah. Verifikasi: lint 0 error, build ✓ (bundle `index-BEmcL7cB.js` live, marker `shift-open-actions`/`f-shift-logout` via curl), test kasir-shell ✓. **Fix review**: cek server di `requestKasirLogout` sebelumnya company-wide (`listShifts status=open` mengembalikan SEMUA shift open) → jika kasir lain punya shift open, kasir yang batal buka shift akan terjebak (modal tutup shift shift orang lain → 404 → tak bisa logout). Kini difilter **per-kasir** (nama/username dari `Auth.user()`) — hanya shift milik kasir login yang menahan logout; kasir tanpa shift langsung logout. Verifikasi akhir: lint 0 error, build ✓ (bundle `index-DKCVYzL9.js` live), test kasir-shell ✓. |
| 0.38 | 2026-08-11 | Freebuff | **Alur Shift Kasir: Wajib Buka Saat Login & Wajib Tutup Sebelum Logout (M6-FIX v3)** — 2 requirement user pada layar kasir: (1) **Kasir login tanpa shift → diarahkan modal Buka Shift otomatis**: setelah `loadKasirData()` selesai di `initPosPage`, jika role kasir (fullscreen) & `!state.shiftAktif` & punya permission `pos.shift.open` → `openShiftModal(true)` langsung muncul melayang di atas halaman kasir (semi abu-abu). Modal bersifat **non-dismissable** (tanpa tombol Batal & `closable:false`, judul "🕐 Buka Shift — Wajib") — kasir TIDAK bisa melewati buka shift untuk bertransaksi; bila submit gagal, modal otomatis dibuka ulang (setTimeout 400ms). Karena kasir sudah login, modal **TANPA dropdown pilihan kasir** (`isKasirMode` → skip fetch `getPosSettings` & `picked=null` → server memakai identitas login via header `x-user-name` + JWT username); mode admin (buka dari widget) tetap menampilkan dropdown bila >1 kasir. (2) **Kasir logout dengan shift belum ditutup → wajib tutup dulu**: tombol Logout header kasir cek shift — jika ada → `closeShiftModal(onDone)` dulu (input actual cash), logout HANYA setelah `closeShift` sukses; tanpa shift → langsung. **v3 hardening (hasil follow-up user + review)**: (a) deteksi kasir TIDAK hanya `shellConfig.fullscreen` — fungsi baru `isKasirLogin()` (export) membaca `Auth.user().role === "kasir"` sebagai sumber kebenaran, dipakai di `initPosPage` (auto modal) & `openShiftModal` (isKasirMode) sehingga kasir yang di-render lewat jalur lain (AppShell) tetap mendapat modal wajib & tanpa dropdown kasir; (b) **guard logout GANDA** — `requestKasirLogout(onDone)` (export dari pages/pos): bila `state.shiftAktif` ada → `closeShiftModal(onDone)`, bila state kosong → cek ke SERVER `listShifts({status:"open"})` sebelum izinkan logout (defense in depth — hasil review), `main.js handleLogout` untuk role kasir memakai `requestKasirLogout(doLogout)` (melindungi jalur AppShell/manapun); (c) tombol Logout kasir memakai `requestKasirLogout`; `closeShiftModal(onDone)` callback dijalankan setelah shift sukses ditutup. Guard permission konsisten dengan `renderShiftWidget`. Verifikasi: lint 0 error, build pos ✓ live sinkron (bundle `index-B9FdvaES.js`; marker `requestKasirLogout`, `Buka Shift — Wajib`, `isKasirLogin` terverifikasi di dist & via curl live), restart pos-api health 200, test kasir shell ✓. Tindakan user: **hard refresh (Ctrl+Shift+R)** lalu login kasir tanpa shift → modal Buka Shift wajib muncul (halaman kasir abu-abu, tanpa pilihan kasir); tekan Logout saat shift terbuka → diarahkan tutup shift dulu. **v3 lanjutan — sinkronisasi nama kasir (user melapor widget tak sinkron)**: (1) akar masalah: mode kasir kirim `openShift(kasAwal,"",{})` tanpa nama → server fallback header `x-user-name` (kosong karena POS tak set session user) → shift tersimpan nama generik "Kasir" → cek `mine` widget gagal → widget tampil "Shift aktif oleh Kasir / Buka Shift Saya". (2) Perbaikan berlapis: server `POST /shift/open` kasir fallback kini `picked || x-user-name || req.user.name || req.user.username || "Kasir"` + `kasirUsername` dari JWT saat picked kosong; `apiCall` kirim `x-user-name` fallback `Auth.user().name` via dynamic import bila session user kosong; **client mode kasir kirim nama EKSPLISIT** `{ name: kasirName, username }` dari `Auth.user()` (tidak bergantung header/JWT); widget `mine` check juga bandingkan `shift.kasirUsername` vs `Auth.user().username` (menutup shift legacy ber-nama generik). `closeShift` server cocokkan via `kasirUsername` JWT → shift legacy tetap bisa ditutup. Verifikasi: lint 0 error, build ✓ (bundle `index-BA7uoYbb.js` live, marker `kasirUsername` via curl), restart pos-api health 200, test pos-gudang & kasir-shell ✓. **v3 lanjutan — otonomi shift tiap kasir (user melapor kasir lain bisa masuk tanpa buka shift)**: akar masalah = fallback legacy `openCount===1` di server — saat kasir #2 login & hanya shift kasir lain (Iswahyuni) yang open, `/kasir-data` mengembalikan shift itu sebagai `shiftAktif` → `state.shiftAktif` terisi → auto-modal wajib tidak terpicu → halaman terbuka langsung & widget tampil "Shift aktif oleh Iswahyuni / Buka Shift Saya". Perbaikan: **hapus kedua fallback legacy di `apps/pos/server/routes/pos.js`** — (1) `/kasir-data`: `shiftAktif` kini STRICT per-kasir (kasirUsername dari JWT / nama dari header); kasir tanpa shift sendiri → `null` → auto-modal "Buka Shift — Wajib" muncul → tiap kasir WAJIB buka shift secara otonom; (2) `/shift/close`: fallback `openCount===1` dihapus + agregasi transaksi selalu difilter per-kasir pemilik shift (kasir lain tidak bisa menutup shift orang lain). Multi-kasir boleh buka shift bersamaan (dedup per-kasir tetap aktif). Verifikasi: syntax OK, lint 0 error, restart pos-api health 200, test pos-gudang & kasir-shell ✓. |
| 0.42 | 2026-08-13 | Freebuff | **Produk VARIAN di Kasir (M6.2-FIX)** — user menemukan 1 produk "Kopi Susu Manis" punya 2 recipe aktif (Pake Gula v2 & Tanpa Gula v1) lalu berkata "saya sengaja buat 2 varian — di kasir tampil 1 kartu produk, saat diorder pilih pake gula atau tanpa gula". Klarifikasi: ini FITUR (bukan bug v0.41 yang saya "perbaiki" — keputusan v0.41 dibatalkan). Desain (keputusan user): **harga beda per varian** + nama item "Produk (Varian)" di keranjang/struk. Implementasi: (1) **Model Recipe** + `harga` (Rp varian; 0 = fallback harga_jual produk); (2) **Aturan diubah**: satu produk boleh punya BANYAK recipe aktif = varian — create/update/duplicate TIDAK lagi meng-archive active lain (hapus invariant v0.41); (3) **Penjualan item** + `recipeId` (varian dipilih) & `applyRecipeConsumption` memakai ingredient recipeId varian (fallback recipe aktif pertama utk data lama); unique index RecipeConsumption jadi (saleId, productId, recipeId) — 2 varian produk sama dalam 1 transaksi keduanya dikonsumsi; (4) **kasir-data** kirim `varian: [{recipeId, nama, harga}]` per produk (dari Recipe aktif); (5) **Kasir UI**: kartu tetap 1 produk + badge `N Varian` + harga "mulai" (varian termurah); klik/scan produk ber-varian → modal **Pilih Varian** (nama + harga masing-masing); keranjang nama "Produk (Varian)", harga varian, recipeId tersimpan; checkout/hold kirim recipeId; resume pulihkan recipeId; reprice member TIDAK menimpa harga varian; (6) **Recipe UI**: form field **Harga Varian** (Rp, kosong = ikut produk), label "Nama Varian", list + kolom Harga (Rp / "ikut produk"), title modal "Edit Varian"; action Edit/Duplikat/Hapus tetap icon. Verifikasi: **full suite 121/121** (7 files, +3 test varian: consumption by recipeId, 2 varian 1 transaksi keduanya dikonsumsi, harga tersimpan; test v0.41 di-rewrite), lint 0 error (4 warning baseline), build pos ✓ (`index-i83Jnc9K.js` live sinkron, marker `Pilih Varian`/`pos-badge-varian`), deploy `pm2 restart pos-api` (health 200), data live: Kopi Susu Manis harga 6000 + 2 varian aktif (harga 0 → fallback 6000; user isi Harga Varian di form utk beda harga), docs pos_execution v0.42. |
| 0.41 | 2026-08-13 | Freebuff | **Fix satu-active-per-produk (DIBATALKAN oleh v0.42)** — user melapor 1 produk (Kopi Susu Manis) muncul 2 varian aktif. Saya "perbaiki" updateRecipe agar selalu archive active lain (invariant satu active per produk). Setelah klarifikasi user, ternyata 2 recipe aktif = VARIAN yang DISENGAJA — keputusan v0.41 di-revert di v0.42 (create/update/duplicate kembali TIDAK archive active lain). |
| 0.40 | 2026-08-13 | Freebuff | **Dijual/Tidak Dijual + Gudang selalu tampil + Aksi Recipe icon (M6.2-FIX)** — 4 requirement user (fokus POS): (1) **Dijual/Tidak Dijual** — barang yang hanya melengkapi resep (gula/rempah) tetap ada di daftar barang tapi tidak dijual; field `dijual:Boolean` (default true, backward compat) di model Barang POS + opsi baru `showDijual` di shared module `BarangModule` (POS meneruskan `showDijual: isTransactionTypeEnabled("fnb")` — kolom/field/badge HANYA tampil bila company mengaktifkan F&B; tanpa F&B (retail/jasa) kolom disembunyikan; inventory default false tidak berubah); form select "Dijual di Kasir" + badge `Dijual`/`Tidak Dijual` (tabel/kartu/detail modal) + fallback offline `barang-data.js` ikut menyimpan `dijual`. (2) **Kasir filter** — server `kasir-data` kini `query.dijual = { $ne: false }` + fallback client kasir `b.dijual !== false`: barang Tidak Dijual tidak tampil di layar kasir. (3) **Recipe action icon saja** — tombol Edit/Duplikat/Hapus diganti icon ✏️/📋/🗑️ (`rc-icon-btn` baru, title tooltip). (4) **Gudang selalu tampil** — field Gudang di form barang kini tampil utk SEMUA tipe (trading/jasa/resep simple/F&B); Rak/Etalase tetap model sebelumnya (hidden utk non-trading); label Gudang "(wajib utk Barang Dagangan)"; validasi submit tetap trading-wajib. Verifikasi: **full suite 118/118** (7 files), lint 0 error (6 warning baseline pre-existing), build pos ✓ (`index-CLyLLEm4.js` live sinkron, marker `badge-tidak-dijual`/`rc-icon-btn` di index + vendor-inv-ui), deploy `pm2 restart pos-api` (health 200), PT-001 transactionTypes `["retail","fnb"]` → kolom Dijual tampil (diverifikasi DB: 12/12 barang dijual default), docs pos_execution v0.40. |
| 0.39 | 2026-08-13 | Freebuff | **Action column Recipe F&B = Edit/Duplikat/Hapus + "Langsung Aktif" (M6.2-FIX)** — user: "fokus POS saja; pada master-recipe F&B kolom action hanya edit, duplikat, hapus; pastikan edit hanya mengubah tanpa duplikat". Keputusan user (ditanya): **Langsung Aktif** — konsep draft/archive dihapus dari UX. Server: (1) `createRecipe` → langsung `status: active` (+ archive active lain utk produk sama — tetap 1 active/produk); (2) `updateRecipe` → SELALU edit in-place (hapus perilaku lama "active → versi draft baru" yang terlihat seperti duplikat), status tetap active; (3) baru `duplicateRecipe` (salinan `name + " (copy)"`, version berikutnya, langsung aktif, active lama di-archive) + `deleteRecipe` (hard delete — aman karena RecipeConsumption simpan snapshot ingredients, reversal void tetap jalan); (4) route baru `POST /api/recipes/:id/duplicate` & `DELETE /api/recipes/:id` (permission pos.recipe.manage, company-scoped). Client (`pages/recipe` + `data/recipe-data`): action column hanya **Edit / Duplikat / Hapus** (Detail/Aktifkan/Archive dihapus + dead code `openDetailModal`/`doActivate`/`doArchive`/`getRecipeCost` dibersihkan); tombol form "Save Draft" → "Simpan Recipe"/"Simpan Perubahan" (edit = langsung berlaku); konfirmasi `window.confirm` utk Hapus. Endpoint activate/archive lama dipertahankan di server (backward compat, tidak dipakai UI). Verifikasi: **full suite 118/118** (7 files, +5 test lifecycle: create→active, update in-place tanpa Recipe.create, duplicate + copy, delete one/false), lint 0 error, build pos ✓ (`index-DelWKlY4.js` live sinkron, marker Duplikat/duplicate), deploy `pm2 restart pos-api` (health 200), route terdaftar (GET /, GET /:id, GET /:id/cost, POST /, PUT /:id, POST /:id/duplicate, DELETE /:id, POST /:id/activate, POST /:id/archive). |
| 0.38 | 2026-08-13 | Freebuff | **Hide Rak/Etalase utk Jasa & semua Resep (M6.2-FIX)** — user: "jika tipe yang dipilih adalah jasa, maka hidden Rak/Etalase, Harga Beli, Stok Awal dan stok minimum; di pos sembunyikan juga jika tipenya resep (simple & F&B)". Shared module form Barang (`@smart/inventory-ui`, dipakai POS & Inventory): field **Rak / Etalase** kini ikut `NON_TRADING_BEHAVIORS` (`["service","recipe","recipe-fnb"]`) — dibungkus `#f-rak-field` dengan inline style `display:none` saat render awal (non-trading) DAN di-toggle di listener `f-behavior` change (sama pola Gudang & `f-trading-fields` Harga Beli/Stok Awal/Stok Minimum yang sudah tersembunyi sejak v0.25). Hasil: tipe **jasa** menyembunyikan Rak/Etalase + Harga Beli + Stok Awal + Stok Minimum di POS & Inventory; tipe **resep simple & F&B** menyembunyikan hal yang sama di POS (dan inventory — karena module shared, walau dropdown resep tidak tampil di inventory). Validasi submit aman (rak tidak wajib; gudang wajib hanya `trading`). Verifikasi: lint 0 error (2 warning baseline pre-existing), build inventory ✓ (`index-ChnFdrZh.js`) + pos ✓ (`index-BP1y6qZK.js`), marker `f-rak-field` ada di vendor shared kedua bundle (2×), bundle live inv & pos sinkron dist (client-only — tanpa restart server), health 200. |
| 0.26 | 2026-08-13 | Freebuff | **Inventory: sembunyikan tipe Resep di form Barang (M6.2-FIX)** — umpan balik user "pada inventory, di modal tambah/edit barang, sembunyikan tipe resep (simple & F&B), sisakan hanya barang dagangan dan jasa (ongkir)". Shared module `BarangModule(services, options)` kini menerima opsi **`behaviorOptions`** (array tipe yang ditampilkan; default semua = `["trading","service","recipe","recipe-fnb"]` — POS tidak berubah). Wrapper inventory meneruskan `behaviorOptions: ["trading","service"]` → dropdown Tipe Barang hanya Barang Dagangan & Jasa. Detail: label tipe memakai katalog `BEHAVIOR_LABELS`; barang existing ber-resep (DB bersama dgn POS) tetap ditampilkan nilainya via opsi `(tipe saat ini)` agar edit tidak diam-diam menjatuhkan ke trading; `NON_TRADING_BEHAVIORS` dipakai utk hide stok/gudang. Verifikasi: lint 0 error (1 warning baseline), build inventory ✓ (`index-D-tDi9Q4.js`, marker `behaviorOptions:["trading","service"]`) + pos ✓ (`index-DVuOEIjs.js`, vendor tetap 11× recipe-fnb = default semua), bundle live inv & pos sinkron dist, health 200. |
| 0.25 | 2026-08-13 | Freebuff | **Dua model resep: simple vs terhubung Recipe F&B (M6.2-FIX)** — umpan balik user "tipe barang Resep/Menu (tanpa stok) tidak muncul di Master Recipe F&B". Konsep: field `behavior` kini membedakan **model SIMPLE** (`recipe` — dijual tanpa kurangi stok produk, bahan TIDAK dikonsumsi realtime, penyesuaian via **stok opname**) dan **model TERHUBUNG** (`recipe-fnb` baru — bahan dikonsumsi **realtime** saat transaksi via engine BOM). Implementasi: (1) **Model Barang POS** — enum `behavior` + `recipe-fnb` (backward compat, additive); (2) **Form Barang shared** (`@smart/inventory-ui`) — opsi "Resep / Menu (Tanpa Stok — simple)" + "Resep / Menu (Terhubung Recipe F&B)", hide stok/gudang utk keduanya, gudang wajib hanya `trading` (fix bug latent: recipe pernah menuntut gudang yang disembunyikan), badge `Resep` vs `Resep F&B` (tabel/kartu/detail); (3) **Halaman kasir** — no-stock & badge utk `recipe-fnb`; (4) **Halaman Master → Recipe F&B** — dropdown Produk HANYA barang `recipe-fnb` + produk yang sudah punya recipe (backward compat data existing, mis. Kopi Selera), hint di modal; (5) **Server enforcement** — `validateRecipePayload` tolak produk `recipe` (simple) 400 dengan pesan jelas; `applyRecipeConsumption` lewati produk `recipe` (simple) — konsumsi realtime hanya `recipe-fnb`/ber-recipe; `NO_STOCK_BEHAVIORS` + `recipe-fnb`; (6) **Test** — 4 test baru (gate simple di konsumsi, resep simple ditolak validasi, recipe-fnb diterima & dikonsumsi). Verifikasi: **full suite 113/113** (7 files POS server), lint 0 error (2 warning baseline pre-existing), build pos ✓ (`index-BFw2EF4u.js`, marker `recipe-fnb` 6×) + inventory ✓ (`index-DGBEQ96X.js`, marker di `vendor-inv-ui-C6FGS9Nw.js`), deploy `pm2 restart pos-api` (health 200), bundle live sinkron dist, docs M6-CAPABILITY-RECIPE.md §Dua model resep diperbarui. |
| 0.37 | 2026-08-11 | Freebuff | **Eye Toggle di Form Register & Reset Password (M3-FIX v28g)** — lanjutan v28e/v28f (follow-up user): perbesar cakupan eye toggle ke form Register & Reset Password agar konsisten. (1) `packages/smart-ui/src/modules/auth/register.js` — field `#reg-password` & `#reg-password-confirm` dibungkus `.password-wrapper` + tombol `#reg-password-toggle`/`#reg-password-confirm-toggle` (👁/🙈); wiring via helper `initPwToggle(inputId, toggleId)` di `initRegisterPageComponent` (guard null aman — tombol hanya dirender saat !isGoogle); CSS `.register-card .form-group .password-wrapper input { padding-right:44px }` (spesifisitas 0,3,1 menang atas `.register-card .form-group input`) + `.password-toggle` ditambahkan di getRegisterStyles. (2) `packages/smart-ui/src/modules/auth/reset-password.js` — `#rp-password` & `#rp-confirm` + toggle `#rp-password-toggle`/`#rp-confirm-toggle`, wiring di `initResetPasswordPage`, CSS di style block halaman. Kedua form dirender langsung ke #app (bukan modal) sehingga style scoped halaman berlaku normal (tidak perlu pola modalStyles v28f). Atribut `autocomplete="new-password"` ditambahkan pada input password register (anti-autofill, konsisten v28d). Verifikasi: lint 0 error (1 warning pre-existing `err` unused di catch handleReset reset-password.js baris 168 — bukan dari perubahan), build pos ✓ (`index-B3n0Gswu.js`) + inv ✓ (`index-BdavSf0i.js`) + console ✓ (`index-CdxO6vs4.js`) live sinkron (marker dist: `reg-password-toggle`, `reg-password-confirm-toggle`, `rp-password-toggle`, `rp-confirm-toggle` + helper bersama), health pos/inv/console 200. **Refactor hasil review (v28g-FIX):** helper eye toggle diekstrak ke `packages/smart-ui/src/modules/auth/password-toggle.js` (`initPasswordToggle(inputId, toggleId)`) — 5 salinan pola yang sama (login.js, settings/user.js, console platform/login.js, register.js, reset-password.js) kini import helper yang sama; auth/index.js mengekspor `initPasswordToggle`; perilaku identik, ~50 baris duplikasi dihapus. Tindakan user: hard refresh (Ctrl+Shift+R) → halaman Daftar & Reset Password → icon mata 👁 di kedua field password. |
| 0.36 | 2026-08-11 | Freebuff | **Fix Posisi Eye Toggle di Modal Tambah/Edit User (M3-FIX v28f)** — lanjutan v28e: user melapor eye toggle di modal user tampil DI BAWAH field password ("Password \* / Password user / 👁"), bukan di dalam field. **Akar masalah**: komponen `Modal` meng-append overlay ke `document.body` (isi modal ada di `.smart-modal-body`), sedangkan style module ditulis scoped `.crud-page` (halaman `#settings-user-page`) → rule `.crud-page .password-toggle { position:absolute; ... }` dan `.crud-page .password-wrapper { position:relative; }` TIDAK menjangkau isi modal → tombol jatuh sebagai button inline biasa di baris bawah input. (Style form modal lain di app pakai scope `.smart-modal-body` di `apps/{pos,inventory}/src/css/main.css` — konvensi yang tidak diikuti oleh CSS eye toggle v28e.) **Perbaikan** (`packages/smart-ui/src/modules/settings/user.js`, module shared): (1) tambah `modalStyles()` — rule `.smart-modal-body .password-wrapper { position:relative }`, `.smart-modal-body .form-group .password-wrapper input { padding-right:42px }` (spesifisitas menang atas `.smart-modal-body .form-group input`), `.smart-modal-body .password-toggle { position:absolute; right:5px; top:50%; translateY(-50%) }` + hover; (2) `renderModal` kini menanam `<style>${modalStyles()}</style>` di awal `content` modal — self-contained, berlaku otomatis utk POS & Inventory tanpa ubah app CSS; (3) dead CSS `.crud-page .password-wrapper/.password-toggle` dihapus dari getStyles (tidak pernah menjangkau modal). Verifikasi: lint 0 error, build pos ✓ (`index-0SW_mYom.js`) + inv ✓ (`index-Cp09sHxh.js`) live sinkron (marker dist: `smart-modal-body .password-wrapper` ada, `crud-page .password-toggle` = 0 — dead code hilang), health pos/inv 200. Fix review: `modalStyles()` dipindah ke module level sejajar `getStyles()` (konsistensi). Tindakan user: hard refresh (Ctrl+Shift+R) → Settings → User → Tambah User → icon mata 👁 kini posisinya DI DALAM field password (kanan). |
| 0.35 | 2026-08-11 | Freebuff | **Eye Toggle Show/Hide Password (M3-FIX v28e)** — 2 requirement user: (1) **Modal Tambah/Edit User** — field password diberi icon mata yang bisa memperlihatkan/menyembunyikan password sehingga admin bisa memverifikasi kebenarannya sebelum simpan; (2) **Form login** — icon mata yang sama pada field password. Implementasi konsisten 3 tempat: (a) `packages/smart-ui/src/modules/settings/user.js` — input `#f-password` dibungkus `.password-wrapper` + tombol `#f-password-toggle` (👁/🙈), wiring di `renderModal` (type password↔text + icon/title swap + refocus input; tombol `type="button"` agar tidak submit via Enter — handler Enter modal tetap aman); (b) `packages/smart-ui/src/modules/auth/login.js` (framework — berlaku login POS & Inventory) — `#login-password` + `#login-password-toggle`, wiring di `initLoginPageComponent`; (c) `apps/console/src/modules/platform/login.js` (login superadmin master.e-profit.id) — `#sa-login-password` + `#sa-login-password-toggle`, wiring di `initSuperAdminLoginPage`. CSS `.password-wrapper` (position relative) + `.password-toggle` (absolute kanan, vertically centered, opacity hover) di ketiga stylesheet; atribut anti-autofill v28d (`autocomplete="new-password" data-lpignore` pada f-password) TETAP dipertahankan. Verifikasi: lint 0 error baru (2 error `google is not defined` di blok Google Sign-In auth/login.js = pre-existing, bukan dari perubahan ini), build pos ✓ (`index-B25LHkNm.js`) + inv ✓ (`index-DifPxCpA.js`) + console ✓ (`index-BSAaZACv.js`) live sinkron (marker dist: `login-password-toggle`×2, `f-password-toggle`×2, `sa-login-password-toggle`×2, `.form-group .password-wrapper input`); health pos/inv/console 200. Tindakan user: hard refresh (Ctrl+Shift+R) → lihat icon mata 👁 di form login & modal Tambah/Edit User → klik untuk menampilkan/menyembunyikan password. |
| 0.34 | 2026-08-11 | Freebuff | **Fix Autofill Browser Mengisi Kotak Pencarian Tabel User (M3-FIX v28d)** — lanjutan v28c: user mengklarifikasi masalah sebenarnya: "tidak sampai simpan — klik Tambah User lalu modal keluar, DI BELAKANG modal data user sudah hilang, hanya tinggal owner yang login". Hasil diagnosa user: kotak pencarian berisi EMAIL yang sedang login (`fejsingtop@gmail.com`) dan baris tersisa = `fejsingtop` (owner). Server & module TERBUKTI bersih (openForm create tidak memicu loadData; state.search hanya berubah via event `input`; bundle live identik source). **Akar masalah (browser)**: Chrome password manager meng-autofill field "username" dengan kredensial login tersimpan saat halaman mengandung field password — modal Tambah/Edit User mengandung `<input type="password" id="f-password">`; saat modal dibuka, Chrome memilih input teks pertama di halaman (`#user-search`, id mengandung kata "user" → dianggap field username oleh heuristik) → diisi `fejsingtop@gmail.com` → event `input` terpacu → filter search aktif → tabel tersisa hanya owner (email cocok hanya utk owner); `autocomplete="off"` TIDAK mencegah autofill password manager. Refresh memperbaiki karena modal belum terbuka — konsisten dgn gejala berulang. **Perbaikan** (module shared `packages/smart-ui/src/modules/settings/user.js` — berlaku pos & inventory): (1) field password modal → `autocomplete="new-password"` (memberi tahu Chrome ini form password BARU, bukan login → autofill kredensial berhenti — **fix utama**); (2) `f-username`/`f-email` → `autocomplete="off"` + `data-form-type="other"` (anti-heuristik autofill); (3) input search → `autocomplete="off" autocapitalize="off" spellcheck="false" data-form-type="other" data-lpignore="true"`; (4) defense-in-depth UX: tombol **× clear filter** (muncul saat filter aktif — state + DOM sinkron; klik = reset search + muat ulang daftar penuh) sehingga filter tak pernah tersembunyi & bisa dihapus 1 klik. Verifikasi: lint 0 error, build pos ✓ (`index-DLp1spl9.js`) & inv ✓ (`index-DvRUpKkp.js`) live sinkron (marker dist: `new-password`×3, `user-search-clear`×3, `data-form-type`×2; display tombol × pakai `flex` agar centering presisi — fix hasil review), health pos/inv 200. Tindakan user: hard refresh (Ctrl+Shift+R) → Settings → User → klik Tambah User → tabel di belakang modal tetap menampilkan SEMUA user; bila kotak pencarian terlanjur terisi dari sesi lama, klik tombol × utk menghapus filter. |
| 0.33 | 2026-08-11 | Freebuff | **Fix Filter Pencarian Tersembunyi di Tabel User (M3-FIX v28c)** — lanjutan v28b: user melapor "saat simpan/edit user, semua user hilang kecuali owner; setelah refresh browser semua muncul lagi; berulang terus". Server TERBUKTI bersih (GET /api/users live selalu mengembalikan 6 user PT-001, sebelum & sesudah PUT edit — direproduksi). **Akar masalah (client, module shared `packages/smart-ui/src/modules/settings/user.js`)**: `state.search` persisten di closure module (dibuat sekali, dirender ulang tiap navigasi) tapi input pencarian di-render TANPA `value` → setelah user mengetik di kotak pencarian (mis. mencari user yang mau diedit), filter tetap aktif saat `loadData()` dipanggil ulang setelah simpan/edit/hapus, sementara kotak pencarian tampak kosong → tabel hanya menampilkan user yang cocok (mis. hanya fejsingtop/owner) → tampak "semuanya hilang". Refresh browser = module dibuat ulang → state.search kosong → semua tampil → berulang. **Perbaikan** (module shared, additive & backward compatible — berlaku pos/inventory/console): (1) input search kini dirender dengan `value="${state.search}"` — filter aktif selalu terlihat di kotak; (2) setelah submit simpan/edit sukses & setelah delete sukses → `state.search = ""; state.page = 1;` sebelum `loadData()` — daftar penuh ditampilkan setelah aksi. Verifikasi: lint 0 error, build pos ✓ (`index-D2nYz45u.js`) & inv ✓ (`index-B2HVh_EL.js`) live sinkron (marker bundle: `user-search... value` di input + `d.search=``,d.page=1` setelah toast sukses), health pos/inv 200. Tindakan user: hard refresh (Ctrl+Shift+R) → Settings → User → simpan/edit → daftar penuh tetap tampil; kotak pencarian kini menampilkan filter aktif bila ada. |
| 0.32 | 2026-08-11 | Freebuff | **Fix Pagination Tabel User — Semua Role Tampil (M3-FIX v28b)** — lanjutan v28: user melapor "semua role kasir sudah tampil, tapi owner/admin/operator menghilang semua". **Akar masalah**: bug pagination pada pola safety-net lama — module mengirim `allCompanies=true` sehingga SERVER mem-page user di SEMUA company (sort `createdAt desc`), lalu client memfilter `companyCode === PT-001` hanya dari data halaman yang diterima. Halaman global 1 (10 user) berisi kasir2/kasir1/kasir + 7 user company lain → setelah filter, hanya 3 user kasir PT-001 yang tampil; `fejsingtop` (owner) & `operator` (dibuat Jul 19) ada di halaman global 2 → tidak pernah tampil, dan `pagination.total` dihitung ulang = 3 → totalPages 1 → tanpa tombol pindah halaman. Diverifikasi live: `GET /api/users?allCompanies=true` → total 12, page1 PT-001 hanya kasir2/kasir1/kasir; `allCompanies=false` + header x-company-code → total 5, page1 = kasir2/kasir1/kasir/fejsingtop(owner)/operator. **Perbaikan** (client-only, pos & inventory `src/data/settings-data.js`): di `listUsers`, bila `currentCompanyCode()` aktif → paksa `params.allCompanies = false` sebelum request → SERVER yang memfilter `companyCode` (dari header x-company-code auto-attach) + paginate per-company; safety-net client dipertahankan (no-op, defense in depth); fallback offline `listUsersLocal` ikut konsisten (`filterData`). Semua pemanggil aman (module `SettingsUserModule` & `_countKasirUsers` wrapper tetap kompatibel). Verifikasi: lint 0 error, build pos ✓ (`index-DxDujDZB.js`) & inv ✓ (`index-C5wo37eG.js`) live sinkron (marker `allCompanies:!1` di dist), GET live allCompanies=false → 5 user semua role, health pos/inv 200. Tindakan user: hard refresh (Ctrl+Shift+R) → Settings → User → owner/operator/admin kini tampil bersama role kasir. |
| 0.31 | 2026-08-11 | Freebuff | **Fix "Role Kasir Tidak Tampil" + E11000 Mentah + Hardening Rate Limiter (M3-FIX v28)** — 2 laporan user: (a) user role kasir lama (kasir1/kasir2) tidak tampil di tabel Settings → User padahal ada di DB (diverifikasi: 5 user PT-001 termasuk kasir1/kasir2); (b) input ulang kasir1 gagal dengan pesan mentah `E11000 duplicate key error ... username_1`. **Akar masalah**: server TIDAK salah — GET /api/users?allCompanies=true live 200 memuat kasir1 (diverifikasi berulang). Yang terjadi: (1) **rate limiter vs nginx** — semua server (pos/inventory/console) berjalan di belakang nginx tanpa `app.set("trust proxy", 1)`; express-rate-limit v7 memanggil validator `xForwardedForHeader` PER-REQUEST di `keyGenerator` → saat nginx mengirim header `X-Forwarded-For`, validator melempar `ERR_ERL_UNEXPECTED_X_FORWARDED_FOR` → error handler 500 sesaat pada request browser (terbukti di log pos-api: puluhan error tepat jam 00:00 saat user mengakses, berhenti setelah restart 00:16) → GET /api/users 500 → `apiListFallback` diam-diam jatuh ke seed lokal `[admin, operator]` (tanpa user kasir) → kasir1 "menghilang" dari tabel; POST pada interval bebas error tetap lolos → user buat ulang → E11000 mentah dari server. **Perbaikan** (4 lapis): (1) **`app.set("trust proxy", 1)`** di `apps/pos/server/index.js`, `apps/inventory/server/index.js`, `apps/console/server/index.js` (trust hop proxy pertama = nginx; rate limiting kini memakai IP klien & validator XFF tidak lagi melempar; diverifikasi: request +X-Forwarded-For → 200 dan hitungan ERR_ERL tidak bertambah setelah restart); (2) **client `listUsers` (POS & inventory) tidak lagi diam-diam memakai seed saat server hidup** — `apps/{pos,inventory}/src/data/settings-data.js`: jika `isApiAvailable()` (health OK) → `apiCall GET /users` langsung + `normalizeList` + safety-net companyCode (error server diteruskan → modul menampilkan Alert, bukan daftar seed yang menyesatkan); fallback seed hanya untuk jaringan down (offline); (3) **pesan E11000 ramah** — `routes/users.js` (pos & inventory) POST/PUT: `friendlyError()` memetakan `code 11000` → `Nilai username duplikat. Username sudah digunakan — muat ulang daftar user untuk melihatnya.`; (4) kuota kasir tetap role-aware (hanya role kasir dihitung — tidak berubah). Verifikasi: lint bersih (hanya warning pre-existing), **full suite 65/65 POS server tests**, build pos ✓ (live `index-BOENU_17.js` sinkron) & inventory ✓, `pm2 restart` ketiga API (health pos/inv/console 200), live GET /api/users 200 memuat kasir1+kasir2+kasir+fejsingtop+operator, POST duplikat → pesan ramah (diverifikasi live), POST owner baru tetap sukses (user uji `verif_fix_v28` dihapus setelah verifikasi). Tindakan user: buka pos.e-profit.id (hard refresh Ctrl+Shift+R) → Settings → User → daftar kini menampilkan kasir/kasir1/kasir2 — tidak perlu input ulang; jika masih belum muncul setelah refresh, login ulang. |
| 0.30 | 2026-08-10 | Freebuff | **Nama Company Mengikuti Master Platform, Bukan Branding App (M3-FIX v27)** — user: "PT-001 kok jadi SMART Kasir? harusnya ikut kode & nama perusahaan di Console (Administration → Companies)". Akar: `ensureCompanyContext()` (main.js:93) memanggil `SMART.Company.set(userCompanyCode, currentInstitution().name)` — `POS_INSTITUTION.name = "SMART Kasir"` (branding app) → field Perusahaan & kolom Company menampilkan "SMART Kasir" alih-alih nama asli. **Perbaikan**: (1) `apps/pos/src/main.js` renderApp — blok fetch logo yang sudah ada (`getCompanyByCode`) kini juga `SMART.Company.set(companyCode, company.name || company.companyName)` bila `!isImpersonating` (impersonasi sudah set nama dari payload smart_imp) → nama company dari Master Platform, reuse fetch (tanpa double call); (2) `apps/inventory/src/main.js` — pola sama (inventory punya kode inline serupa); (3) wrapper settings/user POS & **inventory** — `_currentCompanyName()` kini mendahulukan `SMART.Company.getName?.()` daripada `SMART.Session.get("company.name")` (session bisa stale dari storage lama berisi branding). Kasir page sudah benar sejak v18 (`patchKasirCompanyName` → getCompanyByCode); topbar "SMART Kasir" TETAP = branding app (disengaja). Hasil review: kasir path `SMART.Company` tetap "SMART Kasir" tapi display kasir via patchKasirCompanyName (aman); struk kasir fallback kode (bukan branding). Verifikasi: lint bersih, **full suite 864/864** (48 files), build pos+inventory ✓, deploy live (pos `index-CoG_tvnB.js`, inv `index-DbhgWQ2d.js` sinkron; marker `getName?.()` di dist kedua app), DB PT-001 = "PT. Smart Vision Indotama" (diverifikasi), health pos+inv 200. |
| 0.29 | 2026-08-10 | Freebuff | **Hilangkan Dropdown "Pilih Company" saat Tambah User (M3-FIX v26)** — saat login owner, form Tambah/Edit User masih menampilkan dropdown Company padahal user harus terikat company yang sedang login. **Akar masalah**: `router/routes.js` me-import SEMUA halaman secara static (eager) → wrapper `pages/settings/user` mengeksekusi `SettingsUserModule({ currentCompanyCode: _currentCompanyCode(), ... })` saat BOOT, sebelum login/company context di-set (`SMART.Company`) → `currentCompanyCode` = null → `singleCompanyMode=false` SELAMANYA → dropdown selalu muncul. **Perbaikan** (shared framework, backward compatible): (1) `packages/smart-ui/src/modules/settings/user.js` — konteks company di-resolve LAZY: helper `resolveCompanyCode()`/`resolveCompanyName()`/`isSingleCompany()` menerima string statis ATAU fungsi `() => string` (dievaluasi saat halaman dirender); semua referensi `singleCompanyMode`/`currentCompanyCode`/`currentCompanyName` diganti resolver (loadCompanies, getCompanyName, getCompanyLabel, openForm companyField → disabled input, renderModal fokus, handleSubmit companyCode = company owner). (2) Wrapper POS & **inventory** `pages/settings/user/index.js` — `currentCompanyCode`/`currentCompanyName` kini kirim FUNGSI `() => _currentCompanyCode()`/`_currentCompanyName()` (inventory kena bug tersembunyi yang sama — ikut diperbaiki). Hasil review: try/catch redundan di resolver disederhanakan (wrapper sudah guard); verifikasi `POS_INSTITUTION.id = "PT-001"` & `Auth.user().institution` dari login → company code yang di-bind ke user baru = PT-001 (benar, bukan fallback). Verifikasi: lint bersih, **full suite 864/864** (48 files), build pos+inventory ✓, deploy live (pos `index-xX6ZsOw4.js`, inv `index-BiEQqfms.js` sinkron; marker minify: `currentCompanyCode:()=>Gf()` / `currentCompanyCode:()=>_d()` — fungsi lazy, bukan nilai statis), health pos+inv 200. |
| 0.28 | 2026-08-10 | Freebuff | **Fix Kuota Basi + Mapping Gudang-Kasir Fleksibel (M3-FIX v25)** — 2 requirement user utk PT-001 (company diubah jadi 3 gudang / 5 kasir): (1) **Kuota basi diperbaiki** — akar masalah ganda: (a) BUG `routes/settings.js` `loadContext` query `Company.findOne({ companyCode })` padahal model pakai field `code` → `GET /api/settings` selalu balikin config default (1/1); fix → `{ code: companyCode }` (DB bersama console/POS/inventory sudah berisi jumlahGudang=3, jumlahKasir=5 — diverifikasi live). (b) cache sessionStorage `smart_company_config` (disimpan saat login) tidak refresh → tambah `refreshCompanyConfig()` di `config/company-config.js` (GET /api/settings → saveCompanyConfig, fallback cache) & dipanggil: `main.js bootApp` (admin/owner await, kasir non-blocking), `createGuard` halaman Gudang, `createGuard`+`roleGuard` halaman User — kuota gudang & kasir kini selalu fresh walau Company diubah dari Console tanpa re-login. (2) **Mapping gudang-kasir FLEKSIBEL** — mode lama (checkbox `gudangTerkoneksi` utk kasir tunggal & grid per-kasir 1-gudang) DIHAPUS; `initGudangKasirSetting` ditulis ulang jadi builder baris [pilih gudang][pilih kasir] + tombol 🗑️ per baris + **➕ Tambah Baris** + Simpan: 1 gudang boleh dipakai BANYAK kasir (mis. Store Kebohoran 3 kasir, Gudang Utama 2 kasir), 1 kasir boleh >1 gudang, komposisi bisa diubah kapan saja; seed dari `gudangKasir` (fallback migrasi `gudangTerkoneksi` tiap kasir×tiap gudang, fallback 1 baris kosong); simpan kirim `gudangKasir` rows + `gudangTerkoneksi:[]` (mapping eksplisit). Server `resolveKasirGudang` di-update: gabung SEMUA baris gudangKasir milik kasir (union `kodeList`, backward compatible — single match sama). Hasil review: guard mapper saat 0 user kasir (info box tanpa tombol simpan — cegah wipe config legacy), dead CSS (`ps-gudang-conns/-check/-kasir`) dihapus, hint "Kasir tanpa baris akan melihat seluruh stok". Verifikasi: lint bersih (baru), **full suite 864/864** (48 files, +2 test union), build pos+inventory ✓, deploy live bundle `index-BexDoONr.js` sinkron (marker: `Tambah Baris`, `Kasir tanpa baris...`, `Belum ada user ber-role`; `ps-gudang-check`=0), health pos+inv 200. |
| 0.27 | 2026-08-10 | Freebuff | **Rapikan Struk Normal & Sidebar Kasir (M3-FIX v24)** — 3 requirement user: (1) **Cetak Nota Normal (admin)**: nomor nota kini diberi awalan `No.` — header dokumen menjadi `NOTA PENJUALAN - No. 10082026-0004` (ubah `nomor: item.noInvoice || item.nomor` → `nomor: "No. " + ...` di `printNotaHTML`/`buildPrintDocument`; hanya fungsi nota normal, thermal struk & dokumen lain tak berubah, `<title>` tab ikut konsisten). (2) **Badge Shift kasir**: karena lebar sidebar kiri sempit, tombol `🧾 Tutup` dipindah ke **baris sendiri di bawah teks shift** (`pos-shift-row` baru membungkus dot + teks; `.pos-shift-active` jadi flex-column; tombol `align-self:flex-end` = tetap **mepet kanan**). (3) **Urutan status sidebar kiri** diubah menjadi **Gudang → Pajak → Shift** (blok pajak dipindah ke atas widget shift di `pos-kategori-footer`). Verifikasi: lint bersih (baru; 3 error `Event` & prefer-const pre-existing), test POS 34/34, build pos+inventory ✓, deploy live bundle `index-B-cdzEOJ.js` sinkron (marker live: `pos-shift-row`, `align-self:flex-end`, `nomor:\`No. \`+(e.noInvoice||e.nomor)`, `NOTA PENJUALAN`), health pos+inv 200. |
| 0.26 | 2026-08-10 | Freebuff | **Koreksi v23: status Gudang/Shift/Pajak ke SIDEBAR KIRI (bukan bawah bill)** — user menegaskan maksud "sidebar bawah" = panel Kategori kiri. Dipindahkan ke **`pos-kategori-footer`** di dasar `<aside class="pos-kategori">` (sidebar hijau gradient emerald): baris `🏬 Gudang`, widget Shift (aktif · kas awal + 🧾 Tutup), baris `Pajak (11%) Aktif` — teks putih transparan di atas gradient (pill shift & tax tetap terang/kontras). `.pos-kategori` jadi flex column (list kategori flex:1 scroll), `.pos-kategori-footer` menempel di bawah via margin-top:auto; `.pos-bill-footer` dihapus total; di mobile (≤1100px) footer disembunyikan agar baris kategori horizontal tetap bersih. Area **bill kini benar-benar lega** — keranjang produk terjual terlihat. Verifikasi: lint bersih, semua ID unik (1×), build pos ✓, deploy live bundle `index-IC8Rps8H.js` sinkron (marker `pos-kategori-footer`; `pos-bill-footer` tidak ada di dist), health 200. |
| 0.25 | 2026-08-10 | Freebuff | **Layout Bill Kasir (M3-FIX v23, iterasi pertama — digantikan v0.26)** — kolom bill terlalu penuh; 4 permintaan user: (1) `🏬 Gudang` dipindah ke footer bill (pos-bill-footer); (2) widget Shift dipindah ke footer bill; (3) baris Pajak (11%) dipindah ke footer bill; (4) Jenis Pelanggan naik ke header bill sejajar "Bill" mepet kanan (pos-bill-header). Iterasi ini DIBATALKAN karena user meminta sidebar kiri, bukan bawah bill (lihat v0.26). |
| 0.24 | 2026-08-10 | Freebuff | **Struk Nota Penjualan & Gudang-Kasir (M3-FIX v22)** — 4 requirement user: (1) **Struk kasir: font dibalik** — nama perusahaan kecil (8px), judul `Nota Penjualan` besar (13px bold). (2) **Struk admin (Transaksi→Penjualan) dibuat PERSIS dgn kasir** — `printNotaThermalHTML` ditulis ulang (nama kecil + judul besar, No. Nota/Tgl/Kasir/Gudang/Pelanggan-Member, tabel Nama/Qty/Harga/Subtotal, Subtotal/Diskon/Pajak/TOTAL/Dibayar/Kembali/Metode, Terima Kasih); di-feature-flag `isKasirStruk` (sumber=pos) agar layout SO inventory tidak berubah (fix review). (3) **Pilihan gudang terhubung kasir** (Admin Penjualan, saat gudang > 1): single lokasi → info; kasir tunggal + gudang > 1 → checkbox multi-select gudang terhubung; kasir > 1 + gudang > 1 → **grid mapping Gudang-Kasir** (contoh: Gudang 1-Kasir A, Gudang 2-Kasir C, dst). (4) **Stok scoping penuh per gudang (keputusan PO)**: `Setting` + `gudangTerkoneksi`/`gudangKasir` (per username kasir, case-insensitive); GET/PUT `/api/settings` kirim warehouses aktif + kasirUsers + companyConfig, validasi kode gudang & kasir terdaftar; `kasir-data` resolve gudang kasir (mapping > terkoneksi > null=tanpa filter) & filter `Barang.gudang $in [kode,nama,'']` + `$or gudang:$exists:false`; POST penjualan simpan `kodeGudang`+`gudang`(nama), decrement utamakan `ref.id` (dokumen katalog yg sudah terfilter gudang) lalu fallback scoped; void & delete reversal scoped sama; kasir: chip 🏬 Gudang di bill, payload gudang (checkout & hold), resume pulihkan gudang, struk baris Gudang. Fix review: fallback taxEnabled saat simpan mapping (bukan hardcoded true). Verifikasi: **full suite 862/862** (48 files, +9 test pos-gudang), lint bersih (baru), build pos+inventory ✓, deploy live bundle `index-DWRTZlVp.js` sinkron (marker `Nota Penjualan`, `Mapping Gudang — Kasir`, `Gudang Terhubung Kasir`, `🏬 Gudang`), health pos+inv 200, anon /api/settings 401. |
| 0.23 | 2026-08-10 | Freebuff | **Nota Penjualan & Penyederhanaan Penjualan (M3-FIX v21)** — 4 requirement user: (1) **Struk thermal: judul `Nota Penjualan`** di bawah nama perusahaan (hapus teks `SMART KASIR` pada header struk `printNotaThermal`). (2) **Hapus pilihan cetak di kasir** — setelah checkout langsung `printNotaThermal` (72mm); modal `📋 Cetak Bukti Transaksi` (pilihan Struk Thermal/Normal) dan fungsi lama `printReceipt` + `ensureReceiptNode` dihapus total (dead code bersih). (3) **Tombol Hold 50% + Batal 50%** — `pos-hold-row` kini Hold (flex:1) + `pos-cancel-btn` baru (flex:1, merah) untuk membatalkan transaksi belum dibayar (modal konfirmasi → kosongkan keranjang + reset bayar/diskon/catatan/tipe pelanggan, murni lokal tanpa efek server); badge "ditahan" pindah baris sendiri (`pos-held-row`, width 100%); CSS dark theme ditambah; **Batal selalu tampil walau tanpa permission hold** (review fix). (4) **Admin Transaksi→Penjualan (modul shared, feature-flag `services.isPos` additive — inventory tidak terpengaruh)**: kolom aksi hanya `📋 Nota` / `🚫 Void` / `🗑️` (hapus SO/SJ/Invoice/Kwitansi/Edit); judul kolom `No. SO` → `No. Nota` (tabel penjualan, tabel retur, label form, card retur); tombol `➕ Buat SO Baru` disembunyikan; empty state POS tanpa tombol buat. Hasil review: void hanya utk `sumber=pos & status=paid` (held aman, server 400 utk selain paid), `nomorSO` retur memuat nomor nota sumber. Verifikasi: lint kasir bersih, **full suite 853/853** (47 files, +25 test POS), build pos+inventory ✓, deploy live bundle `index-gI-x7VUP.js` sinkron (marker `Nota Penjualan`, `Batal Transaksi`, `Buat transaksi baru dari layar Kasir`, `No. Nota`), health pos+inv 200. |
| 0.22 | 2026-08-10 | Freebuff | **Verifikasi Member & Cetak Bukti Thermal (M3-FIX v20)** — 2 requirement user: (1) **Pilih member → scan kartu / input kode** — klik `🎫 Member` membuka modal Verifikasi Member: scan kartu via kamera (reuse `attachScanner` dgn selektor khusus `[data-scan-member]` agar tidak bentrok dgn scanner produk `[data-scan-index]`) atau input kode + Enter/tombol Verifikasi; validasi `getMemberByKode` (API `/customer/check-kode/:kode`, fallback lokal); sukses → `state.member {kode,nama}` + tipe member + keranjang di-re-price harga khusus; transaksi menyimpan `pelanggan`=kode member + `pelangganNama`=nama member (server validasi member AKTIF di Customer — 400 bila kosong/tidak ditemukan; simpan nama dari Master Member, bukan client); hold simpan member & resume pulihkan; struk thermal & normal menampilkan nama+kode member (untuk laporan transaksi member / program member). **Fix hasil review:** `GET /api/customer/check-kode` kini memfilter `active:true, status≠archived` (konsisten dgn validasi POST penjualan — mencegah verifikasi sukses lalu checkout 400); klik Member saat sudah member → buka ulang modal (ganti member); state member di-reset setelah hold (transaksi baru). (2) **Cetak bukti transaksi model nota thermal** — setelah checkout sukses muncul modal `📋 Cetak Bukti Transaksi` (meniru "Cetak Nota" di Transaksi→Penjualan): `🧾 Cetak Struk Thermal` (layout 72mm, mirror printNotaThermalHTML admin: header company, No. Nota, kasir, member, item, subtotal/diskon/pajak/total, dibayar, kembalian, metode) atau `📄 Cetak Normal` (struk lama `printReceipt`). Verifikasi: lint 0 (baru), **full suite 853/853** (47 files), build pos ✓, deploy pos-api online (health 200), live bundle `index-thEpvkHj.js` sinkron (marker `Verifikasi Member`, `Scan kartu member`, `Cetak Bukti Transaksi`, `Cetak Struk Thermal`), anon /api/penjualan 401. |
| 0.21 | 2026-08-10 | Freebuff | **Nota, Pajak Admin, Member & Harga Khusus (M3-FIX v19)** — 4 requirement user: (1) **Nomor Nota** — alur kasir hanya "nota" (bukan SO/SJ/Invoice/Kwitansi): `Penjualan.generateNomor` dukung prefix kosong → `DDMMYYYY-XXXX` (contoh `No. Nota: 10082026-0001`); POST `/api/penjualan` sumber=pos pakai prefix nota; struk kasir label `No. Nota:`; tombol/cetak di halaman Penjualan utk item `sumber=pos` = **Nota thermal 72mm** (printKwitansiThermal) + judul NOTA. (2) **Pajak diatur Admin** — model `Setting` baru (companyCode unique, taxEnabled) + route `GET/PUT /api/settings` (PUT permission `settings.company.edit`, upsert per company); radio **Aktif/Off** di menu Transaksi → Penjualan (modul shared PenjualanModule, feature-flag `deps.getPosSettings` — inventory tidak terdampak); toggle kasir **DIHAPUS** (status statis dari kasir-data `taxEnabled`, tarif tetap 11%). (3) **Rename Pelanggan → Member** — menu Master + halaman `Master Member` (kode contoh MBR-001). (4) **Harga khusus hanya member** — kasir-data kini `harga` = harga_jual (normal) + `harga_khusus` terpisah; layar kasir punya pilihan **Jenis Pelanggan: Umum (default) / Member**; `effectivePrice`: member → `harga_khusus` bila >0, umum → `harga_jual`; keranjang di-re-price saat ganti tipe; transaksi simpan `tipePelanggan` (enum umum/member) + `pelangganNama` Member/"Pelanggan Umum" (helper pure `normalizeTipePelanggan` + 3 test). **Hasil review di-fix:** boolean `PUT /settings` toleran ("false"/0 = off), `GET /settings` wajib header company (cegah lintas company), cetak Nota admin → thermal 72mm utk item POS. Verifikasi: lint 0 (baru), **full suite 853/853** (47 files; +3), build pos+inventory+console ✓, deploy pos-api online (health 200), live bundle `index-BMXf0ETb.js` sinkron (marker `No. Nota`, `Jenis Pelanggan`, `Pajak Transaksi (layar kasir)`), anon /api/settings 401. **Catatan keputusan:** harga khusus bersifat per-item global (`Barang.harga_khusus`); toggle Member dipilih kasir tanpa picker member spesifik (sesuai request — tidak mencatat member mana). |
| 0.20 | 2026-08-10 | Freebuff | **Fix Hold/Resume 500 & 400 (M3-FIX v18)** — umpan balik user (console error live): `POST /api/penjualan failed: Jumlah bayar kurang dari total transaksi` lalu `POST /api/penjualan/3/hold` → 500 `Cast to ObjectId failed for value "3"`. **Root cause (2 lapis):** (1) `holdCurrentCart` mengirim `sumber:"pos"` + `bayar:0` → server menolak 400 (POS wajib lunas) — padahal ini HANYA hold draft; (2) `apiCreateFallback` (shared `apiFetch`) **menelan semua error termasuk 4xx** → fallback ke data lokal → id palsu `"3"`/`"4"` → `POST /hold` di server → CastError 500. **Fix server (apps/pos):** POST `/api/penjualan` dukung flag **`hold:true`** (helper pure `resolvePosCreateFlags`): skip validasi bayar + skip decrement stok, dibuat langsung `status:"held"` + heldAt/By/Note, tanpa noKwitansi (prefix SO), satu request atomik; route `/:id/hold` & `/:id/resume` + guard `ObjectId.isValid` → 400 (bukan 500 CastError); **PATCH /:id/status kini ditolak untuk sumber="pos"** (POS langsung lunas via checkout — mencegah hold→resume→PATCH mengubah draft bayar 0 jadi paid tanpa pembayaran; jalur yang benar: void). **Fix client (apps/pos):** kasir `holdCurrentCart` single-call `hold:true` (hapus panggilan `holdPenjualan` terpisah); `createPenjualan` **rethrow error server** (fallback lokal HANYA saat jaringan down / API null — pola `isNetworkError` sama seperti void; mencegah transaksi palsu ber-id lokal) + `normalizeItem` (_id→id) agar pemanggil lain tetap kompatibel; `createPenjualanLocal` dukung `hold:true` (parity offline). Verifikasi: lint 0, **full suite 850/850** (47 files; +4 test `resolvePosCreateFlags`), build pos ✓, deploy pos-api (online, health 200), live bundle `index-BT5obRbO.js` sinkron dist (marker `holdNote` ada), anon hold/resume/PATCH tetap 401. |
| 0.19 | 2026-08-10 | Freebuff | **Kuota kasir ROLE-AWARE (M3-FIX v17, POS & Inventory)** — umpan balik user "pembatasan jumlah kasir cukup hanya pada role kasir saja, jangan semua user dibatasi". Audit → **server sudah benar** (routes/users.js POST & PUT hanya memblokir saat role `kasir`, PUT exclude `_id` yang diedit); yang salah = **UI guard global**: saat kuota penuh, tombol `➕ Tambah User` dinonaktifkan TOTAL sehingga admin/operator juga tidak bisa dibuat. Fix di shared module `SettingsUserModule` (`packages/smart-ui`, additive & backward-compatible): (1) opsi baru **`roleGuard(role, ctx)`** — diblokir hanya saat role tertentu (kasir) penuh; `handleSubmit` await `checkRoleGuard(selectedRole, { isEdit, currentRole })` sebelum create/edit; (2) `updateGuardUI` kini menampilkan banner info meski `allowed:true` (pesan kuota role-aware); (3) halaman Settings User POS & Inventory: `createGuard` mengembalikan `{ allowed: true, message: "Kuota kasir tercapai (X/Y). Role lain tetap bisa ditambahkan." }` (tombol TETAP AKTIF — hanya banner), `roleGuard` hanya memblokir role `kasir`. **Post-review hardening (code-reviewer):** (a) **edit user kasir existing TIDAK diblokir** — `roleGuard` menerima konteks edit (`isEdit` + `currentRole`): edit user kasir yang sudah terhitung kuota (ganti nama/password, role tetap kasir) diizinkan, sejalan dgn server PUT exclude `_id`; (b) **hitung kasir scoped perusahaan** — `_countKasirUsers` kini filter `companyCode` aktif (sebelumnya `allCompanies:true` menghitung lintas perusahaan → kuota palsu di multi-company). Verifikasi: lint 0, **full suite 846/846** (47 files), build pos+inventory+console ✓, deploy pos-api + inventory-api (online, health 200), live bundle `index-NGSZUoHI.js` (pos) & `index-Cb5-oTAP.js` (inv) — marker `Role lain tetap bisa ditambahkan` + `roleGuard gagal` terverifikasi, anon /api/users 401 (server tetap enforce). |
